---
phase: 07-ui-simplification-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [QUAL-01, QUAL-02]
must_haves:
  truths:
    - "The final v1.1 addon tree differs from the exact pre-Phase 5 baseline in only the six approved runtime files."
    - "Every Phase 5 and Phase 6 focused regression and every applicable WotLK 3.3.5a static gate passes before full discovery runs."
    - "Exactly one final-tree unittest discovery executes all 512 tests through the existing LuaJIT-backed runner and passes."
    - "Automated evidence proves protected repository contracts without claiming any Screen Notice, Trade, Loot, Logger, or Attendance live observation."
  artifacts:
    - path: ".planning/phases/07-ui-simplification-verification/07-01-SUMMARY.md"
      provides: "Sanitized automated acceptance evidence with exact baseline and final Git hashes, commands, counts, and dispositions"
    - path: "Raid Management Addon/Raid Management Addon.toc"
      provides: "Authoritative Interface 30300 load order and ordered six-key SavedVariables declaration"
      contains: "RMA_Raids, RMA_Players, RMA_Reserves, RMA_Warnings, RMA_Spammer, RMA_Options"
    - path: "tests/lua_test_runner.py"
      provides: "Existing LuaJIT-compatible execution path used by the complete Python suite"
  key_links:
    - from: "539a651d961bdfab25a7a7ebf849041f1a3ec75e"
      to: "final Phase 7 verification commit"
      via: "Exact Git production-diff allowlist and protected-surface zero-diff checks"
      pattern: "Controllers/Attendance.lua|Controllers/Logger.lua|Modules/UI/ListController.lua|Modules/UI/ScreenNotice.lua|Services/Loot/State.lua|Services/Master/Trade.lua"
    - from: "tests/test_runtime_foundations_behavior.py"
      to: "tests/lua/runtime_harness.lua"
      via: "tests/lua_test_runner.py executes the Screen Notice, shared-list, and Attendance Lua cases"
      pattern: "run_lua_case"
    - from: "tests/test_sync_communications_behavior.py"
      to: "Raid Management Addon/Controllers/Logger.lua"
      via: "Logger controller-edge parity case uses the real shared list primitives"
      pattern: "logger_lists_use_shared_layout_primitives_without_behavior_drift"
---

<objective>
Produce the reproducible automated half of the v1.1 acceptance record on one fixed final tree.

Purpose: Establish QUAL-01 and the automated/protected-surface portion of QUAL-02 without expanding the test framework, changing compatibility contracts, or treating automation as live-client proof.
Output: `07-01-SUMMARY.md` containing the exact baseline/final range, protected surfaces, command forms, validator and test counts, and unambiguous automated dispositions.
</objective>

<execution_context>
@C:/Users/Massimo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Massimo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-ui-simplification-verification/07-CONTEXT.md
@.planning/phases/05-runtime-surface-cleanup/05-VERIFICATION.md
@.planning/phases/06-logger-and-attendance-list-primitives/06-VERIFICATION.md
@AGENTS.md
@.agents/skills/wow-addon-dev-wotlk-v335a/SKILL.md
@Raid Management Addon/Raid Management Addon.toc
@tests/lua_test_runner.py
@tests/test_runtime_foundations_behavior.py
@tests/test_runtime_bootstrap_contract.py
@tests/test_loot_distribution_hardening_behavior.py
@tests/test_sync_communications_behavior.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the final range and prove the production scope</name>
  <files>None (read-only Git comparison; the output step creates 07-01-SUMMARY.md)</files>
  <action>Set the fixed pre-Phase 5 baseline to the full hash `539a651d961bdfab25a7a7ebf849041f1a3ec75e`, resolve the current final hash with `git rev-parse HEAD`, and require the baseline to be an ancestor of that final hash. Record both full hashes; do not use a moving branch name as report evidence. Inspect `git diff --name-status $baseline..$final -- 'Raid Management Addon'` and require the production path set to equal exactly these six modified files with no additions, deletions, or renames: `Controllers/Attendance.lua`, `Controllers/Logger.lua`, `Modules/UI/ListController.lua`, `Modules/UI/ScreenNotice.lua`, `Services/Loot/State.lua`, and `Services/Master/Trade.lua` below the addon folder.

Run explicit zero-diff checks over the same fixed range for all addon XML, `Raid Management Addon.toc`, `Localization/`, `Libs/`, the complete `Database/` directory, `Modules/Comms.lua`, `Services/Reserves/Sync.lua`, `Services/Loot/DistributionSession.lua`, `EntryPoints/`, `Init.lua`, `Raid Management Addon/README.md`, and `docs/API_SURFACE.md`. This protects layout, localized text, the six SavedVariables and persistence owners, all version-5 wire owners, vendored source, current public bootstrap/slash entrypoints, and their declared documentation by byte-exact Git comparison. Separately record the test-file diff and planning-file diff so test/evidence changes are not confused with production changes. Do not edit a protected surface or reinterpret an exact-diff failure as functional equivalence. If the range or allowlist fails, stop with the exact paths and an `INCONCLUSIVE`/`FAIL` result before running tests.</action>
  <verify>Require `git merge-base --is-ancestor $baseline $final` to exit 0. Require the sorted addon diff to equal the six-file allowlist exactly. Require `git diff --quiet $baseline $final --` over each protected path group to exit 0. Run `git diff --check $baseline..$final` and require no whitespace errors. Preserve the exact commands or reproducible PowerShell forms and full hashes for the summary.</verify>
  <done>The acceptance range is immutable and auditable; only the six intended runtime files changed, while XML, TOC, localization, persistence, wire owners, Libs, entrypoints, and supported-surface documentation are byte-identical to the pre-Phase 5 baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Pass focused regressions and WotLK static gates</name>
  <files>None (read-only verification; the output step creates 07-01-SUMMARY.md)</files>
  <action>Use the bundled Python executable `C:\Users\Massimo\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe`. Before any Lua-backed test, create a self-contained shim outside the repository under a task-specific temporary directory: copy `C:\tools\LuaJIT\bin\luajit.exe` to `lua.exe`, copy `C:\tools\LuaJIT\bin\lua51.dll` beside it, prepend that directory to the current process PATH, and preflight `shutil.which('lua')`. Do not modify the repository runner or place an alias in the repository.

Run the six milestone-focused Python unittest methods together: the Screen Notice internal-event case, manual Trade inventory-evidence/retry case, direct Loot runtime-state normalization case, shared-list geometry case, Logger controller-edge parity case, and Attendance controller-edge parity case. Require 6/6 passing through the existing Lua runner.

Only after the focused cases pass, run the repository-owned TOC validator against `Raid Management Addon/Raid Management Addon.toc`, Lua 5.1 lint against `Raid Management Addon` and `tests/lua`, and the variadic-`xpcall` scan against `Raid Management Addon`. Scan non-vendored XML for script handlers by constructing the actual angle-bracket regex in PowerShell. Scan non-vendored runtime Lua for `C_Timer`, `C_AddOns`, `Settings.`, `MenuUtil`, `SetAtlas`, `SetColorTexture`, `_ENV`, `table.pack`, `table.unpack`, and `bit32`; let `lint_lua51.py` remain authoritative for syntax such as `goto`, integer division, and bitwise operators. Repeat the Phase 4 proprietary-runtime UTF-8 scan, excluding only `Localization/` and `Libs/`, and require zero code points above ASCII. Inventory direct `SetScript("OnUpdate", ...)` calls and require exactly the established shared scheduler in `Modules/Timer.lua`; separately inspect added lines in the fixed v1.1 production diff and require no new `OnUpdate`, frame driver, timer, cache, metatable, or compatibility layer.

Run exact current-contract assertions using read-only PowerShell or inline Python: Interface remains `30300`; the ordered SavedVariables declaration remains exactly the six canonical keys; version communication, raid history, Reserves, and Distribution remain protocol version 5 with their established RMA-prefixed owners; `_G["RMA"]`, the sole `/rma` slash owner, and documented public entrypoint files remain unchanged from the fixed baseline. Finish the static stage with `git diff --check`. Record actual counts and zero-match/exact-inventory results, not merely command exit codes.

If a check cannot execute because the Python/LuaJIT environment is unavailable, record `INCONCLUSIVE` and stop; never convert unavailable tooling into PASS. If a gate demonstrates a simplification regression, capture the narrow failure and stop this acceptance plan before full discovery so a correction can target only the evidenced owner. Do not add speculative tests, validators, fallbacks, compatibility layers, protocol changes, migrations, or unrelated refactors. Do not exceed two focused repair cycles for the same evidenced cause in any follow-up correction.</action>
  <verify>Run the focused methods `RuntimeFoundationsBehaviorTest.test_screen_notice_uses_internal_event_without_direct_export`, `LootDistributionHardeningTests.test_manual_hold_trade_requires_inventory_evidence`, `RuntimeBootstrapContractTest.test_loot_runtime_state_syncs_directly_without_service_forwarder`, `RuntimeFoundationsBehaviorTest.test_ui_lists_preserve_explicit_layout_geometry`, `SyncCommunicationsBehaviorTests.test_logger_lists_use_shared_layout_primitives_without_behavior_drift`, and `RuntimeFoundationsBehaviorTest.test_attendance_lists_use_shared_layout_primitives_without_behavior_drift`; require 6/6. Invoke `validate_toc.py`, `lint_lua51.py`, and `scan_xpcall.py` with the bundled Python and require exit 0 with recorded counts. Require zero prohibited XML, modern API, Lua 5.2+, proprietary non-ASCII, and v1.1-added polling/framework matches; require exact current RMA identity, version-5 wire, Interface 30300, and six-key SavedVariables assertions; require `git diff --check` clean.</verify>
  <done>All narrow behavior and static compatibility gates are green on the fixed final tree, with counts captured and no live behavior inferred.</done>
</task>

<task type="auto">
  <name>Task 3: Run the single final 512-test discovery</name>
  <files>None (read-only verification; the output step creates 07-01-SUMMARY.md)</files>
  <action>Confirm Tasks 1 and 2 are fully green and the same fixed final Git hash is still checked out. With the external LuaJIT shim still first on PATH, run complete unittest discovery exactly once as the final automated acceptance gate: use the bundled Python with `-m unittest discover -s tests -p 'test_*.py'`. Require exactly 512 tests discovered, all 512 passing, and confirm the Lua-backed cases executed through the existing runner rather than being skipped, replaced, or weakened.

If this full discovery fails, do not edit, rerun discovery, or claim partial acceptance inside this plan. Stop with the exact failing methods and output so a separate evidenced correction can rerun the nearest focused checks and static gates before any replacement final-tree discovery. In the standard summary output, record test count, pass count, duration, fixed final hash, focused/static evidence, protected surfaces, and the unambiguous `AUTOMATED` disposition. State explicitly that Screen Notice, Trade, and Loot live checks remain pending Plan 07-02 and that the approved Phase 6 Logger/Attendance observations have not been rerun or relabeled by automation.</action>
  <verify>Run the complete discovery once and require `Ran 512 tests`, `OK`, and exit 0. Verify the recorded final hash still equals the Task 1 final hash. The summary must contain the baseline/final range, command forms, validator/test counts, protected-surface results, `AUTOMATED` classification, and no claim of new live observation.</verify>
  <done>QUAL-01 and the automated portion of QUAL-02 have one final-tree 512/512 result after every focused and static gate, with live evidence boundaries preserved.</done>
</task>

</tasks>

<verification>
- The fixed baseline/final addon diff contains exactly the six intended v1.1 runtime files and no protected-surface drift.
- All six Phase 5/6 focused regressions pass through the existing LuaJIT-compatible runner before complete discovery.
- TOC, Lua 5.1, variadic-xpcall, XML, modern-API, ASCII, runtime-hygiene, identity, persistence, wire, and diff gates meet their exact or zero-match contracts.
- Complete unittest discovery runs once only after all focused/static gates and reports exactly 512/512 passing.
- `07-01-SUMMARY.md` records only automated evidence and leaves live Screen Notice, Trade, and Loot observations to Plan 07-02.
</verification>

<success_criteria>
- QUAL-01 has reproducible evidence from the complete 512-test suite and every applicable WotLK 3.3.5a static validator.
- QUAL-02 has byte-exact baseline evidence for XML, TOC, localization, persistence, wire owners, Libs, and public entrypoints, plus focused behavior evidence for all six changed owners.
- No unavailable tool, deferred live scenario, or prior observation is promoted into a new PASS.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-simplification-verification/07-01-SUMMARY.md`. Include the exact baseline/final hashes, reproducible command forms, focused/static/full-suite counts, protected surfaces, `AUTOMATED` disposition, and explicit pending-live boundary required above.
</output>

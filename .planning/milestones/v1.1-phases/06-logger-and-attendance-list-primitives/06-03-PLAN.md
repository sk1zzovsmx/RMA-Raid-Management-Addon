---
phase: 06-logger-and-attendance-list-primitives
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - Raid Management Addon/Controllers/Attendance.lua
  - tests/lua/harness/30_raid_runtime.lua
  - tests/test_runtime_foundations_behavior.py
autonomous: true
requirements: [UI-03]
must_haves:
  truths:
    - "Attendance raid and player lists retain exact calculated widths, header/row positions, one-time sort binding, contextual titles, and selected empty-state text through shared primitives."
    - "Attendance Spec and Inspect share only supplied header/container geometry; icon creation/placement, textures, slot rendering, status, tooltips, force-inspect behavior, and interaction remain Attendance-owned."
    - "Attendance keeps all layout constants, raid/player descriptor tables, sorters, localized text selection, refresh points, and controller orchestration."
    - "The final Phase 6 tree changes no XML, localization, Libs, TOC, SavedVariables, wire format, public entrypoint, or feature behavior and passes all focused/static/full-suite gates."
  artifacts:
    - path: "Raid Management Addon/Controllers/Attendance.lua"
      provides: "Attendance integration with focused shared list primitives while retaining Spec/Inspect presentation ownership"
      contains: "local ATTENDANCE_LAYOUT_COLUMNS"
    - path: "tests/lua/harness/30_raid_runtime.lua"
      provides: "Focused Attendance geometry, sort, title, empty-state, and Spec/Inspect ownership regression"
      contains: "attendance_lists_use_shared_layout_primitives_without_behavior_drift"
    - path: "tests/test_runtime_foundations_behavior.py"
      provides: "Focused Python registration and controller/shared-owner boundary assertions"
      contains: "test_attendance_lists_use_shared_layout_primitives_without_behavior_drift"
  key_links:
    - from: "Raid Management Addon/Controllers/Attendance.lua"
      to: "Raid Management Addon/Modules/UI/ListController.lua"
      via: "Attendance passes local constants, width maps, descriptors, and explicit geometry to focused Lists operations"
      pattern: "UI.Lists"
    - from: "Raid Management Addon/Controllers/Attendance.lua"
      to: "Raid Management Addon/Services/EquipInspect.lua"
      via: "Attendance-owned Spec/Inspect rendering and ForcePlayer interaction remain outside shared list primitives"
      pattern: "ForceInspectPlayer"
---

<objective>
Route Attendance list mechanics through the tested shared operations, retain all Spec/Inspect ownership, and close Phase 6 on one final compatibility pass.

Purpose: Complete the two-controller consolidation without changing visible behavior or expanding the shared abstraction.
Output: A mechanically smaller Attendance controller, focused ownership regression, and final WotLK/Lua/full-suite evidence.
</objective>

<execution_context>
@C:/Users/Massimo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Massimo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-logger-and-attendance-list-primitives/06-CONTEXT.md
@.planning/phases/06-logger-and-attendance-list-primitives/06-01-SUMMARY.md
@.planning/phases/06-logger-and-attendance-list-primitives/06-02-SUMMARY.md
@AGENTS.md
@Raid Management Addon/Modules/UI/ListController.lua
@Raid Management Addon/Controllers/Attendance.lua
@tests/lua/harness/30_raid_runtime.lua
@tests/test_runtime_foundations_behavior.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED - capture Attendance geometry and Spec/Inspect ownership</name>
  <files>tests/lua/harness/30_raid_runtime.lua, tests/test_runtime_foundations_behavior.py</files>
  <action>Add `cases.attendance_lists_use_shared_layout_primitives_without_behavior_drift` by extending the existing real Attendance controller fixture, and register it in `RuntimeFoundationsBehaviorTest`. Instrument the Plan 01 shared operations with preserving spies and extend fake widgets/rows only enough to record widths, anchors, scripts, text, visibility, and Spec/Inspect render observations.

Exercise both Attendance controller configs through localization, representative row drawing, and post-update. Assert exact shared results for valid and invalid ScrollFrame widths: explicit fallback 240, gutter 24, row inset 3, five/three gaps of 6, header start offset 6, top offset -25, minimum/floor/remainder behavior inherited from the shared contract, matching header/row widths, and no extra/hit-box variant for Attendance. Invoke localization twice and prove sortable headers bind once to Attendance-owned sort keys; Inspect remains unsortable because its descriptor has no sort key.

Assert raid title/empty state remains exact `Raids (n)` with the controller-selected empty text; player title remains exact `Raid Attendees (n) - Context` with unmodified context and no Logger fallback when context is absent; player empty-state choice remains controller-owned. Exercise player row rendering sufficiently to prove the shared row operation applies only Name/Join/Leave/Ilvl/Spec/InspectStatus widths before Attendance continues its loot-ban name inset, Spec icon selection/texture/tooltip path, Inspect status/slot icon creation and placement, and interactions. The Python method must also assert `AttendanceLayout`, both local descriptor tables (including the intentionally duplicated raid table), `setAttendanceSpecIcon`, `renderAttendanceInspectIcons`, tooltip helpers, `RAID_INSPECT_SLOTS`, and Force Inspect wiring remain in `Attendance.lua` and are absent from `ListController.lua`.

Run RED against unchanged Attendance after Plans 01-02 and require failure specifically because the shared-operation spies show Attendance still uses duplicated local mechanics. Commit the failing regression only as `test(06-03): lock Attendance list presentation parity`.</action>
  <verify>In PowerShell, create/preflight the external Lua shim before RED: `$shimDir = Join-Path ([System.IO.Path]::GetTempPath()) 'rma-phase06-lua'; New-Item -ItemType Directory -Force -Path $shimDir | Out-Null; Copy-Item -LiteralPath 'C:\tools\LuaJIT\bin\luajit.exe' -Destination (Join-Path $shimDir 'lua.exe') -Force; Copy-Item -LiteralPath 'C:\tools\LuaJIT\bin\lua51.dll' -Destination (Join-Path $shimDir 'lua51.dll') -Force; $env:PATH = "$shimDir;$env:PATH"; $pythonExe = 'C:\Users\Massimo\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe'; &amp; $pythonExe -c "import shutil; assert shutil.which('lua')"; &amp; $pythonExe -m unittest tests.test_runtime_foundations_behavior.RuntimeFoundationsBehaviorTest.test_attendance_lists_use_shared_layout_primitives_without_behavior_drift`. Confirm `lua.exe` and `lua51.dll` are side by side outside the repo, the fixture reaches Attendance, and RED fails specifically on absent shared primitive calls, not on Spec/Inspect setup, unrelated roster behavior, or environment.</verify>
  <done>A committed focused Attendance regression observes exact geometry/binding/title behavior and proves the intended Spec/Inspect ownership boundary before extraction.</done>
</task>

<task type="auto">
  <name>Task 2: Route Attendance mechanics through shared primitives and close final gates</name>
  <files>Raid Management Addon/Controllers/Attendance.lua</files>
  <action>Assert/cache the seven Plan 01 `UI.Lists` operations near Attendance's existing UI bindings. Replace only duplicated content-width, budget, header, row-width, sort-binding, title formatting, and label application helpers/calls. Pass all values explicitly from `AttendanceLayout`: fallback 240, gutter 24, inset 3, gap 6, panel/header offsets, gap counts, local minimum/ratio maps, local `RAID_LAYOUT_COLUMNS`/`ATTENDANCE_LAYOUT_COLUMNS`, and the existing explicit bound flags.

Keep `AttendanceLayout`, both descriptor tables (including the duplicate raid descriptors), all minimums/ratios/sort keys/sorters, localization calls, context/empty-state selection, loot-ban name-width adjustment, Spec/Inspect icon creation/caches/textures/slots/placement/tooltips/status, Force Inspect actions, and localization/row/post-update recalculation points in Attendance. Shared title formatting receives the already selected localized base/context and no fallback context. Shared label application receives only the controller-selected text. Remove only redundant local mechanical helpers; do not add wrapper layers, new call points, caches, listeners, timers, or polling.

Run the focused Attendance regression to GREEN. In this same task, define the bundled Python executable and create a self-contained LuaJIT shim outside the repository before any Lua-backed Python command: copy `C:\tools\LuaJIT\bin\luajit.exe` to `<temp-shim>\lua.exe`, copy `C:\tools\LuaJIT\bin\lua51.dll` beside it, prepend that directory to PATH, and preflight `shutil.which('lua')`; do not create an alias inside the repo. Then run focused Logger and shared-primitive methods together once to detect cross-controller regression. After focused tests are green, run the static gates below, diagnose only evidenced failures with the nearest focused rerun, and allow at most two repair cycles for one cause without new evidence. Finally run complete unittest discovery exactly once as the last automated command and require 512/512 (the 509-test Phase 5 baseline plus exactly three Phase 6 registrations). Do not blindly rerun the full suite; if it fails, stop and report the exact evidence so any correction culminates in one replacement final run on a new final tree. Commit the Attendance production change atomically as `refactor(06-03): share Attendance list mechanics`; SUMMARY/STATE/ROADMAP remain the standard metadata commit.</action>
  <verify>Start this task's verification in PowerShell with the complete external-runner setup: `$shimDir = Join-Path ([System.IO.Path]::GetTempPath()) 'rma-phase06-lua'; New-Item -ItemType Directory -Force -Path $shimDir | Out-Null; Copy-Item -LiteralPath 'C:\tools\LuaJIT\bin\luajit.exe' -Destination (Join-Path $shimDir 'lua.exe') -Force; Copy-Item -LiteralPath 'C:\tools\LuaJIT\bin\lua51.dll' -Destination (Join-Path $shimDir 'lua51.dll') -Force; $env:PATH = "$shimDir;$env:PATH"; $pythonExe = 'C:\Users\Massimo\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe'; &amp; $pythonExe -c "import shutil; assert shutil.which('lua')"`. Confirm `(Test-Path -LiteralPath (Join-Path $shimDir 'lua.exe')) -and (Test-Path -LiteralPath (Join-Path $shimDir 'lua51.dll'))` is true and no shim file was created in the repository. First run `&amp; $pythonExe -m unittest tests.test_runtime_foundations_behavior.RuntimeFoundationsBehaviorTest.test_attendance_lists_use_shared_layout_primitives_without_behavior_drift` and require PASS. Then run exactly `&amp; $pythonExe -m unittest tests.test_runtime_foundations_behavior.RuntimeFoundationsBehaviorTest.test_ui_lists_preserve_explicit_layout_geometry tests.test_sync_communications_behavior.SyncCommunicationsBehaviorTests.test_logger_lists_use_shared_layout_primitives_without_behavior_drift tests.test_runtime_foundations_behavior.RuntimeFoundationsBehaviorTest.test_attendance_lists_use_shared_layout_primitives_without_behavior_drift` and require 3/3. Confirm feature ownership with `rg -n "AttendanceLayout|RAID_LAYOUT_COLUMNS|ATTENDANCE_LAYOUT_COLUMNS|setAttendanceSpecIcon|renderAttendanceInspectIcons|RAID_INSPECT_SLOTS|ForceInspectPlayer" "Raid Management Addon/Controllers/Attendance.lua"` and confirm `rg -n "RAID_INSPECT_SLOTS|setAttendanceSpecIcon|renderAttendanceInspectIcons|ForceInspectPlayer|LOGGER_|ATTENDANCE_" "Raid Management Addon/Modules/UI/ListController.lua"` returns no matches.

Before full discovery, invoke every validator with the bundled executable and full repository-owned script path: `&amp; $pythonExe '.agents/skills/wow-addon-dev-wotlk-v335a/scripts/validate_toc.py' 'Raid Management Addon/Raid Management Addon.toc'`; `&amp; $pythonExe '.agents/skills/wow-addon-dev-wotlk-v335a/scripts/lint_lua51.py' 'Raid Management Addon' 'tests/lua'`; and `&amp; $pythonExe '.agents/skills/wow-addon-dev-wotlk-v335a/scripts/scan_xpcall.py' 'Raid Management Addon'`. Build the XML regex with actual angle-bracket characters rather than Markdown entities: `$xmlPattern = ([char]60) + 'Scripts' + ([char]62) + '|' + ([char]60) + 'On[A-Za-z]+' + ([char]62); rg -n $xmlPattern 'Raid Management Addon' -g '*.xml' -g '!**/Libs/**'`; require no matches. Run `rg -n 'C_Timer|C_AddOns|Settings\.|MenuUtil|SetAtlas|SetColorTexture' 'Raid Management Addon' -g '*.lua' -g '!**/Libs/**'` and require no new matches. Run `git diff --check`; inspect Phase 6 task commits and require no XML, Localization, Libs, TOC, Database/SavedVariables, communication/wire owner, or non-Logger/Attendance feature file change. Then, as the final automated command only, with the temp shim (not `C:\tools\LuaJIT\bin`) still first on PATH and `lua51.dll` beside its `lua.exe`, run `&amp; $pythonExe -m unittest discover -s tests -p 'test_*.py'` and require exactly 512 tests, all passing.</verify>
  <done>Attendance is GREEN through shared mechanics with Spec/Inspect ownership intact; all focused and static gates pass; the complete final suite passes exactly once at 512/512; and the Phase 6 diff stays inside the approved runtime/test/planning surface.</done>
</task>

</tasks>

<verification>
- Attendance RED/GREEN proves extraction rather than merely source similarity, and Logger/shared focused regressions remain green.
- Numeric parity covers fallback, gutter, floor/remainder, offsets/gaps, headers/rows, sort binding, contextual titles, and empty states across both controllers.
- Logger Source availability/hit box and Attendance Spec/Inspect behavior remain controller-owned and observable.
- TOC, Lua 5.1 addon+harness lint, variadic-`xpcall`, XML layout-only, forbidden-modern-API, scope, and whitespace gates are clean before the one final full-suite run.
- No XML, localization, Libs, TOC, SavedVariables, wire format, public entrypoint, resize listener, cache, timer, polling, framework, DSL, or feature behavior changes.
</verification>

<success_criteria>
- UI-03 is protected by exact Attendance controller-edge regression, and UI-01/UI-02 remain green through their focused cases.
- Logger and Attendance share only stable mechanics from `addon.UI.Lists`; controller presentation ownership remains explicit.
- Exactly 512/512 tests pass on the final Phase 6 tree after all applicable WotLK 3.3.5a gates are clean.
</success_criteria>

<output>
After completion, create `.planning/phases/06-logger-and-attendance-list-primitives/06-03-SUMMARY.md`, then advance `.planning/STATE.md`, record decisions/metrics/session continuity, and update `.planning/ROADMAP.md` through the standard execute-plan workflow. This is the last Phase 6 plan, so STATE and ROADMAP must mark the phase complete and ready for Phase 7 verification without pre-completing QUAL-01 or QUAL-02.
</output>

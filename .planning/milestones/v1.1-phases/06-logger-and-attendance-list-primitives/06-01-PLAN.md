---
phase: 06-logger-and-attendance-list-primitives
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - Raid Management Addon/Modules/UI/ListController.lua
  - tests/lua/harness/40_inspect_foundations.lua
  - tests/test_runtime_foundations_behavior.py
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "Shared list calculations preserve the exact 240 fallback, scrollbar gutter subtraction, floor behavior, explicit lead/gap/returned-width offsets, existing proportional allocation, and remainder distribution."
    - "Shared header and row operations apply only controller-supplied geometry, including an explicit extra header width and an explicitly named hit box, without hidden visual defaults."
    - "Shared sort binding installs each supplied sortable header once through UI.Frames.SetScriptSafely and dispatches the controller-owned sort key."
    - "Shared title and optional-label operations preserve exact count/context formatting and apply only caller-selected text and visibility."
    - "The shared owner remains a focused extension of addon.UI.Lists, not a framework, DSL, orchestrator, cache, resize listener, timer, or polling path."
  artifacts:
    - path: "Raid Management Addon/Modules/UI/ListController.lua"
      provides: "Focused calculated-width, header, row, sort-binding, title, and optional-label primitives under addon.UI.Lists"
      contains: "function Lists.CalculateColumnWidths"
    - path: "tests/lua/harness/40_inspect_foundations.lua"
      provides: "Deterministic numeric and binding parity coverage for the shared list primitive contract"
      contains: "ui_lists_preserve_explicit_layout_geometry"
    - path: "tests/test_runtime_foundations_behavior.py"
      provides: "Focused Python registration for the Lua-backed shared primitive regression"
      contains: "test_ui_lists_preserve_explicit_layout_geometry"
  key_links:
    - from: "Raid Management Addon/Modules/UI/ListController.lua"
      to: "Raid Management Addon/Modules/UI/Frames.lua"
      via: "The shared sort operation uses the already bound Frames.SetScriptSafely owner"
      pattern: "Frames.SetScriptSafely"
    - from: "Raid Management Addon/Modules/UI/ListController.lua"
      to: "Raid Management Addon/Modules/UI/Visuals.lua"
      via: "The optional-label operation delegates visibility to the existing Primitives.SetShown owner"
      pattern: "SetShown"
---

<objective>
Extend `addon.UI.Lists` with the smallest explicit primitives needed by Logger and Attendance, with deterministic parity proven before controller migration.

Purpose: Establish one tested shared contract for duplicated geometry and binding mechanics while keeping every visual constant, descriptor, localized string, and presentation decision in its controller.
Output: Focused `UI.Lists` operations plus a Lua-backed RED/GREEN regression for exact geometry, binding, titles, and optional labels.
</objective>

<execution_context>
@C:/Users/Massimo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Massimo/.codex/get-shit-done/templates/summary.md
@C:/Users/Massimo/.codex/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-logger-and-attendance-list-primitives/06-CONTEXT.md
@AGENTS.md
@Raid Management Addon/Modules/UI/ListController.lua
@Raid Management Addon/Modules/UI/Frames.lua
@Raid Management Addon/Modules/UI/Visuals.lua
@Raid Management Addon/Controllers/Logger.lua
@Raid Management Addon/Controllers/Attendance.lua
@tests/lua/harness/40_inspect_foundations.lua
@tests/test_runtime_foundations_behavior.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED - lock the explicit shared list contract</name>
  <files>tests/lua/harness/40_inspect_foundations.lua, tests/test_runtime_foundations_behavior.py</files>
  <action>Add `cases.ui_lists_preserve_explicit_layout_geometry` beside the existing ListController harness coverage and register it as `RuntimeFoundationsBehaviorTest.test_ui_lists_preserve_explicit_layout_geometry`. Reuse the existing Lua harness and fake-frame style; do not create a test framework.

Before changing production, record the current `Lists.CalculateColumnWidths` results for representative Logger raid, Logger loot, and Attendance column tables and hard-code those maps as parity expectations. Cover minimum-only output, fixed keys, `floor` of proportional additions, and the final pixels assigned in the exact current `pairs`-derived key order observed from the unmodified LuaJIT runtime. Then specify the new public contract by calling focused `addon.UI.Lists` functions named `GetContentWidth`, `CalculateColumnBudget`, `ApplyHeaderLayout`, `ApplyRowWidths`, `BindSortHeaders`, `FormatCountTitle`, and `SetLabel`.

The regression must assert: a valid ScrollFrame width wins; an invalid width falls back to frame width minus the explicit 24 gutter; absent/invalid widths return the explicit 240 fallback; all results are floored and clamped to 240; the Logger loot budget preserves `width - 34 - (5 * 6) + 30`; header offsets start from the caller's explicit start/top values and advance by width plus the explicit 6 gap; `headerExtraWidthKey = "icon"` adds icon width only to the header; `hitBoxKey = "SourceHitBox"` receives exactly the Source row width; missing widgets are tolerated; two binding attempts with the same explicit flag install each sortable OnClick once through `SetScriptSafely`; clicks dispatch the captured sort keys; `FormatCountTitle("Title", 7)` returns `Title (7)`; context returns `Title (7) - Context`; an empty context uses the explicit Logger fallback when supplied and no suffix when omitted; `SetLabel` writes caller-selected text and, only when its explicit visibility argument requests it, uses `Primitives.SetShown` for empty/non-empty state.

Run the focused method against unchanged production and require RED to fail because at least the new focused operation (starting with `UI.Lists.GetContentWidth`) is nil/missing. It must not fail from fixture setup, an altered `CalculateColumnWidths` baseline, a missing Lua runner, or an unrelated assertion. Commit the failing regression only as `test(06-01): define shared list primitive contract`.</action>
  <verify>In PowerShell, create the Lua runner shim outside the repository and preflight it before the focused test: `$shimDir = Join-Path ([System.IO.Path]::GetTempPath()) 'rma-phase06-lua'; New-Item -ItemType Directory -Force -Path $shimDir | Out-Null; Copy-Item -LiteralPath 'C:\tools\LuaJIT\bin\luajit.exe' -Destination (Join-Path $shimDir 'lua.exe') -Force; Copy-Item -LiteralPath 'C:\tools\LuaJIT\bin\lua51.dll' -Destination (Join-Path $shimDir 'lua51.dll') -Force; $env:PATH = "$shimDir;$env:PATH"; $pythonExe = 'C:\Users\Massimo\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe'; &amp; $pythonExe -c "import shutil; assert shutil.which('lua')"; &amp; $pythonExe -m unittest tests.test_runtime_foundations_behavior.RuntimeFoundationsBehaviorTest.test_ui_lists_preserve_explicit_layout_geometry`. Confirm `lua.exe` and `lua51.dll` are side by side under `$shimDir`, the method reaches LuaJIT, and RED fails specifically on a missing new `UI.Lists` operation while every assertion against the pre-existing `CalculateColumnWidths` baseline before that call passes. Do not create or modify any repository file for the alias.</verify>
  <done>A committed focused regression captures the exact existing numeric baseline and fails for the intended absence of the new shared primitive API, not for environment or fixture noise.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN - add focused primitives under addon.UI.Lists</name>
  <files>Raid Management Addon/Modules/UI/ListController.lua</files>
  <action>Implement only the seven focused public operations exercised by RED under the existing `Lists` table, using Lua 5.1 syntax and the module's existing `_G`, `floor`, `Frames`, and `Primitives` bindings:

- `GetContentWidth(frameName, fallbackWidth, gutterWidth)` must read `<frameName>ScrollFrame:GetWidth()` when positive, otherwise read the frame width and subtract only the caller-supplied gutter when the frame is wider than it, then `floor` and clamp to the caller-supplied fallback (240 in both consumers).
- `CalculateColumnBudget(frameName, fallbackWidth, gutterWidth, leadOffset, gapCount, gapWidth, returnedWidthOffset)` must call `GetContentWidth`, subtract the explicit lead and `gapCount * gapWidth`, add the explicit returned-width offset, then floor/clamp to fallback. Do not hide 24, 240, 3, 6, 30, or 34 in this module.
- `ApplyHeaderLayout(frameName, widths, columns, startOffset, topOffset, gapWidth)` must resolve only `headerSuffix`, `widthKey`, `headerExtraWidthKey`, and `trailingGap`; clear/anchor each existing header to the supplied frame and set its width to the calculated header width plus the supplied trailing gap. No layout callback, renderer, default descriptor, or all-in-one orchestration.
- `ApplyRowWidths(ui, widths, columns)` must set the supplied `rowKey` widget width and, only when supplied, the `hitBoxKey` widget to the same non-header width. It must not know about icons, Source availability, Spec, Inspect, or feature state.
- `BindSortHeaders(frameName, columns, listRef, boundFlag)` must validate the frame and `listRef.Sort`, return if the explicit flag is already set, bind each supplied `sortKey` once through `Frames.SetScriptSafely`, and set only that caller-selected frame flag. Do not supply a hidden flag name.
- `FormatCountTitle(baseText, count, contextText, fallbackContext)` must preserve exactly `Title (n)` and `Title (n) - Context`, using the explicit fallback only when context is nil/empty and returning the count title alone when both are empty.
- `SetLabel(frameName, suffix, text, manageVisibility)` must set only the resolved label text (`text or ""`); when `manageVisibility == true`, call the existing `Primitives.SetShown` with whether the supplied text is a non-empty string. It must not select localization or empty-state conditions.

Leave `CalculateColumnWidths` byte-for-byte behaviorally equivalent, especially its minimum accumulation, fixed-key exclusion, `pairs`-derived variable-key order, floor allocation, and remainder loop. Add no metatable, object, configuration DSL, second convenience layer, resize listener, `OnUpdate`, timer, cache, new dependency, or visual default. Re-run the focused test to GREEN, make only obvious local cleanup that does not enlarge the API, and commit production atomically as `feat(06-01): add focused shared list primitives`.</action>
  <verify>In PowerShell, recreate/preflight the external shim before GREEN: `$shimDir = Join-Path ([System.IO.Path]::GetTempPath()) 'rma-phase06-lua'; New-Item -ItemType Directory -Force -Path $shimDir | Out-Null; Copy-Item -LiteralPath 'C:\tools\LuaJIT\bin\luajit.exe' -Destination (Join-Path $shimDir 'lua.exe') -Force; Copy-Item -LiteralPath 'C:\tools\LuaJIT\bin\lua51.dll' -Destination (Join-Path $shimDir 'lua51.dll') -Force; $env:PATH = "$shimDir;$env:PATH"; $pythonExe = 'C:\Users\Massimo\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe'; &amp; $pythonExe -c "import shutil; assert shutil.which('lua')"; &amp; $pythonExe -m unittest tests.test_runtime_foundations_behavior.RuntimeFoundationsBehaviorTest.test_ui_lists_preserve_explicit_layout_geometry` and require PASS. Confirm `lua.exe` and `lua51.dll` are side by side and no alias file exists in the repo. Then run `rg -n "function Lists\.(GetContentWidth|CalculateColumnBudget|ApplyHeaderLayout|ApplyRowWidths|BindSortHeaders|FormatCountTitle|SetLabel)" "Raid Management Addon/Modules/UI/ListController.lua"` and require exactly the seven focused exports. Run `rg -n "OnUpdate|ScheduleTimer|C_Timer|resize|cache|metatable" "Raid Management Addon/Modules/UI/ListController.lua"`; only pre-existing controller refresh machinery may appear, with no new layout listener/cache/framework path in the diff. Run `git diff --check`.</verify>
  <done>The shared owner passes the exact numeric and binding contract while exposing only explicit focused operations and retaining the existing allocation algorithm.</done>
</task>

</tasks>

<verification>
- RED fails for the missing focused API before any production edit; GREEN passes after the minimal implementation.
- Exact tests cover fallback 240, gutter subtraction, floor/remainder allocation, gap/offset arithmetic, extra header width, hit-box parity, one-time sort binding, count/context titles, and optional-label visibility.
- `ListController.lua` contains no controller descriptors/constants, localized text, feature renderer, visual default, DSL, orchestrator, resize listener, timer, polling loop, or new cache.
- Do not run complete unittest discovery in this plan; the full suite is reserved for the final Phase 6 plan.
</verification>

<success_criteria>
- UI-01 has a small, deterministic shared primitive contract under the established `addon.UI.Lists` owner.
- All geometry and presentation policy remains explicit at call sites.
- The focused Lua-backed regression is green on LuaJIT and `git diff --check` is clean.
</success_criteria>

<output>
After completion, create `.planning/phases/06-logger-and-attendance-list-primitives/06-01-SUMMARY.md`, then advance `.planning/STATE.md`, record decisions/metrics/session continuity, and update `.planning/ROADMAP.md` through the standard execute-plan workflow.
</output>

---
phase: 03-bounded-sync-requests
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - Raid Management Addon/Services/Loot/DistributionSession.lua
  - tests/lua/harness/10_loot_distribution.lua
  - tests/test_loot_distribution_hardening_behavior.py
autonomous: true
requirements: [COMM-02, COMM-03, COMM-04]
must_haves:
  truths:
    - "The first valid authorized SNAP_REQ produces the same RMADist protocol-version-5 snapshot response and remains independent from reserve request admission state."
    - "A repeated SNAP_REQ from the same canonical sender is handled silently before PublishSnapshot, snapshot encoding, chunk construction, or transport, then becomes eligible at exactly five seconds without rejected replays extending expiry."
    - "Distribution admission uses one transient owner-local map capped at 128 active canonical senders; aliases share entries, active entries are not evicted, and lazy exact-boundary expiry frees capacity."
    - "Malformed, mistargeted, invalid-version, invalid-identity, or non-member requests do not consume state; an admitted no-publish request does consume state; the first active-cooldown rejection alone uses the shared debug diagnostic."
  artifacts:
    - path: "Raid Management Addon/Services/Loot/DistributionSession.lua"
      provides: "Distribution-owned transient SNAP_REQ admission state and pre-PublishSnapshot guard"
      contains: "DistributionSession.HandleMessage"
    - path: "tests/lua/harness/10_loot_distribution.lua"
      provides: "Exact boundary, normalization, capacity, authorization, no-work, and R5 snapshot response regressions"
      contains: "loot_distribution_snapshot_requests_are_rate_limited_before_response_work"
    - path: "tests/test_loot_distribution_hardening_behavior.py"
      provides: "Python registration for the focused distribution Lua behavior case"
    - path: "Raid Management Addon/Localization/DiagnoseLog.en.lua"
      provides: "Shared debug-only rate rejection template created by Plan 03-01"
      contains: "LogSyncRequestRateLimited"
  key_links:
    - from: "Raid Management Addon/Services/Loot/DistributionSession.lua"
      to: "Raid Management Addon/Services/Loot/DistributionSession.lua"
      via: "DistributionSession.HandleMessage calls the owner-local admission helper after closed R5 decode and raw-sender membership authorization, before DistributionSession.PublishSnapshot"
      pattern: "admitResponseRequest"
    - from: "Raid Management Addon/Services/Loot/DistributionSession.lua"
      to: "Raid Management Addon/Modules/Comms.lua"
      via: "Comms.NormalizeSender plus case folding only for the admission key"
      pattern: "NormalizeSender"
    - from: "Raid Management Addon/Services/Loot/DistributionSession.lua"
      to: "Raid Management Addon/Localization/DiagnoseLog.en.lua"
      via: "Plan 03-01's shared debug diagnostic without shared admission state"
      pattern: "LogSyncRequestRateLimited"
---

<objective>
Bound loot-distribution snapshot response work per canonical sender while preserving the distribution protocol and authorization path.

Purpose: Prevent authorized SNAP_REQ floods from entering snapshot construction or transport without coupling distribution state to reserves or raid-history sync.
Output: Distribution-owner admission state plus focused snapshot and final cross-owner compatibility regressions.
</objective>

<execution_context>
@C:/Users/Massimo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Massimo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bounded-sync-requests/03-CONTEXT.md
@.planning/phases/03-bounded-sync-requests/03-RESEARCH.md
@.planning/phases/03-bounded-sync-requests/03-01-SUMMARY.md
@Raid Management Addon/Services/Loot/DistributionSession.lua
@Raid Management Addon/Modules/Comms.lua
@Raid Management Addon/Database/DBSyncSession.lua
@Raid Management Addon/Localization/DiagnoseLog.en.lua
@tests/lua/harness/10_loot_distribution.lua
@tests/test_loot_distribution_hardening_behavior.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Specify the distribution snapshot admission boundary before runtime changes</name>
  <files>tests/lua/harness/10_loot_distribution.lua, tests/test_loot_distribution_hardening_behavior.py</files>
  <action>Extend `createDistributionSessionFixture` only as needed with mutable original-sender membership and `CanUseCapability` results, the Plan 03-01 `Diag.D.LogSyncRequestRateLimited` template plus debug enable/capture, a wrapper counter around `DistributionSession.PublishSnapshot`, a response-only `Payload.Serialize` counter after inbound messages are prebuilt, and existing queue attempt/sent counters. Add a separate Lua case `loot_distribution_snapshot_requests_are_rate_limited_before_response_work` and register it as `test_distribution_snapshot_requests_are_rate_limited_before_response_work` in `LootDistributionHardeningTests`; leave the existing outgoing request-correlation/incoming-assembly bound case intact and do not edit runtime code in this task.

Cover the locked matrix: unchanged first response prefix `RMADist`, version 5, `SNAP`/`SNAP_CHUNK` shape, original request ID, sender target, and `NORMAL` priority; same-kind rejection at `t + 4.999`; rejected replay non-extension; admission at exactly `t + 5.000`; `Player`, `player`, and `Player-Realm` aliasing; malformed body, wrong target/version, invalid normalization, and non-member non-consumption; authorization re-evaluation from the raw sender with leave/re-entry not clearing cooldown; an admitted request consuming the slot when `CanUseCapability("loot")` makes `PublishSnapshot` return false; debug-disabled rejection with no debug/chat/warning output and exactly one debug line when enabled for an active cooldown; 128 active sender keys, unseen sender 129 rejected without eviction, and lazy exact-boundary expiry freeing capacity. Inspect `DistributionSession._requestAdmissions` to prove the hard map bound. Assert every rejection returns handled `true` and causes zero `PublishSnapshot` entry, response serialization, chunk construction, queue attempt, or sent packet. Also assert the distribution owner has its own map and does not reference reserve or DBSync admission state. Commit this test-only task atomically as `test(03-02): specify distribution request admission boundary`.</action>
  <verify>Run `lua tests/lua/runtime_harness.lua loot_distribution_snapshot_requests_are_rate_limited_before_response_work` and confirm the new case fails against the unmodified distribution runtime for the missing admission behavior. Also run `python -m unittest tests.test_loot_distribution_hardening_behavior.LootDistributionHardeningTests.test_distribution_snapshot_requests_are_rate_limited_before_response_work`. If the known Lua executable remains unavailable, record only that environment constraint, confirm the case and Python registration with `rg -n "loot_distribution_snapshot_requests_are_rate_limited_before_response_work|test_distribution_snapshot_requests_are_rate_limited_before_response_work" tests`, and retain every required assertion for later Lua/in-game execution.</verify>
  <done>A focused test contract exists for every distribution boundary, identity, capacity, authorization, debug-dedupe, no-work, handled-rejection, and R5 compatibility requirement, with runtime source still unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Admit distribution snapshot requests before PublishSnapshot</name>
  <files>Raid Management Addon/Services/Loot/DistributionSession.lua</files>
  <action>In `Services/Loot/DistributionSession.lua`, beside the existing protocol constants and transient `_incomingSnapshots`/`_pendingSnapshots` state, add owner-local `REQUEST_COOLDOWN_SECONDS = 5`, `MAX_REQUEST_SENDERS = 128`, and the transient sender-keyed table `DistributionSession._requestAdmissions`. Implement the Lua 5.1 local helper `admitResponseRequest(originalSender, requestKind)` and only its small prune/count helpers, reproducing the timestamp-map behavior locally instead of importing reserve or `DB.SyncSession` code: derive a non-empty lowercase short-name key through the established `Comms.NormalizeSender(originalSender)` semantics; lazily remove kind state when `now - admittedAt >= 5`; remove empty sender entries; count no more than 128 active sender keys; reject an unseen sender at capacity without eviction or overflow state; leave `admittedAt` unchanged on rejection; and keep the one `rejectionLogged` flag inside the active `SNAP_REQ` state. Do not change the broader `normalizeWireName` wire/correlation semantics.

In `DistributionSession.HandleMessage`, call admission only for a successfully decoded/validated `MSG_SNAPSHOT_REQ`, after `IsGroupMember(Raid, sender)` succeeds and immediately before dispatch can reach `DistributionSession.PublishSnapshot(sender, requestId)`. Keep the original sender for membership on every request and use normalization only for the rate key. Return `true` on invalid identity, active cooldown, or capacity rejection so the recognized message cannot fall through. Admit before `PublishSnapshot` and its `canPublish()` check so valid authorized no-publish requests consume their slot. Reuse Plan 03-01's `Diag.D.LogSyncRequestRateLimited` for only the first active-cooldown debug event with service label, kind, and canonical sender; keep capacity-rejected unseen senders silent because no bounded entry exists. Preserve `PREFIX = "RMADist"`, `PROTOCOL_VERSION = 5`, all decode/target/body rules, snapshot encoding/chunking, request correlation, priorities, authority behavior, SavedVariables, TOC, and timer behavior; add no shared admission module, timer, ticker, or `OnUpdate`. Commit atomically as `fix(03-02): bound distribution snapshot requests`.</action>
  <verify>Run both focused Lua cases and their Python methods: `lua tests/lua/runtime_harness.lua reserves_sync_incoming_requests_are_rate_limited_before_response_work`, `lua tests/lua/runtime_harness.lua loot_distribution_snapshot_requests_are_rate_limited_before_response_work`, and the two focused `python -m unittest` method paths. When Lua is available, all must pass; when it is absent, preserve the tests and report the runner limitation as environmental rather than a product failure. Then run the relevant owner suites once with `python -m unittest tests.test_sync_communications_behavior tests.test_loot_distribution_hardening_behavior`, followed by `python .agents/skills/wow-addon-dev-wotlk-v335a/scripts/validate_toc.py "Raid Management Addon/Raid Management Addon.toc"`, `python .agents/skills/wow-addon-dev-wotlk-v335a/scripts/lint_lua51.py "Raid Management Addon"`, `python .agents/skills/wow-addon-dev-wotlk-v335a/scripts/scan_xpcall.py "Raid Management Addon"`, `rg -n "C_Timer|OnUpdate|ScheduleTimer" "Raid Management Addon/Services/Reserves/Sync.lua" "Raid Management Addon/Services/Loot/DistributionSession.lua"`, `rg -n "&lt;Scripts&gt;|&lt;On[A-Za-z]+&gt;" "Raid Management Addon/UI" -g "*.xml"`, and `git diff --check`. Timer and XML handler searches must show no new matches.</verify>
  <done>Distribution `SNAP_REQ` work is admitted once per canonical sender per inclusive five-second boundary, retained in at most 128 transient owner-local entries, rejected before snapshot/queue work, debug-deduplicated through the shared key, and both R5 owners retain unchanged normal behavior.</done>
</task>

</tasks>

<verification>
- Both new owner-focused Lua cases pass with a Lua 5.1-compatible executable; the current runner absence remains an explicit environment constraint and does not reduce COMM-04's required behavior matrix.
- Source inspection confirms closed R5 validation and raw-sender membership precede distribution admission, while admission precedes `PublishSnapshot` and every serialization/queue surface.
- The focused owner suites, TOC validator, Lua 5.1 lint, `xpcall` scan, no-new-timer scan, XML handler scan, and `git diff --check` are clean apart from the documented missing Lua runtime gate.
- `RMAResSync` and `RMADist`, protocol version 5, envelope/body shapes, correlation, targeting, priorities, authorization, SavedVariables, and owner independence remain unchanged.
</verification>

<success_criteria>
- Normal reserve and distribution requests receive their existing compatible first responses without cross-kind or cross-service throttling.
- Repeated `META_REQ`, `DATA_REQ`, and `SNAP_REQ` messages are stopped at the owner boundary before response construction or queue allocation and re-admitted exactly at expiry.
- Sender aliases cannot obtain separate budgets; both owner maps and debug dedupe remain transient and capped at 128 active senders each.
- COMM-01 through COMM-04 have executable regression coverage even where the current shell cannot execute Lua.
</success_criteria>

<output>
After completion, create `.planning/phases/03-bounded-sync-requests/03-02-SUMMARY.md`.
</output>

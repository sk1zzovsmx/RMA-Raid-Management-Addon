---
phase: 03-bounded-sync-requests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Raid Management Addon/Services/Reserves/Sync.lua
  - Raid Management Addon/Localization/DiagnoseLog.en.lua
  - tests/lua/harness/50_reserves_messaging.lua
  - tests/test_sync_communications_behavior.py
autonomous: true
requirements: [COMM-01, COMM-03, COMM-04]
must_haves:
  truths:
    - "The first valid authorized META_REQ and DATA_REQ from one sender are both admitted at the same time and produce their unchanged protocol-version-5 reserve responses."
    - "A second request of the same kind from the same canonical sender performs no payload construction, serialization, chunk construction, or transport work before five seconds, while an exact t + 5.000 request is admitted."
    - "Case and Name-Realm aliases share one cooldown, rejected attempts do not extend it, and reserve admission retains at most 128 active sender entries without evicting an active sender."
    - "Malformed, mistargeted, invalid-version, invalid-identity, or unauthorized requests consume no admission state; an admitted no-data request does consume state; ordinary rejection is silent and debug rejection logs once per active sender/kind cooldown."
  artifacts:
    - path: "Raid Management Addon/Services/Reserves/Sync.lua"
      provides: "Reserve-owned transient META_REQ and DATA_REQ admission state and pre-response dispatch guards"
      contains: "Sync:HandleMessage"
    - path: "Raid Management Addon/Localization/DiagnoseLog.en.lua"
      provides: "One shared ASCII debug-only request-rate diagnostic template"
      contains: "LogSyncRequestRateLimited"
    - path: "tests/lua/harness/50_reserves_messaging.lua"
      provides: "Exact boundary, normalization, capacity, authorization, no-work, and wire-compatibility reserve regressions"
      contains: "reserves_sync_incoming_requests_are_rate_limited_before_response_work"
    - path: "tests/test_sync_communications_behavior.py"
      provides: "Python registration for the focused reserve Lua behavior case"
  key_links:
    - from: "Raid Management Addon/Services/Reserves/Sync.lua"
      to: "Raid Management Addon/Services/Reserves/Sync.lua"
      via: "Sync:HandleMessage calls the owner-local admission helper inside each validated request branch immediately before sendMetadata or sendData"
      pattern: "admitResponseRequest"
    - from: "Raid Management Addon/Services/Reserves/Sync.lua"
      to: "Raid Management Addon/Modules/Comms.lua"
      via: "Comms.NormalizeSender followed by case folding for the bounded key while raw sender remains the authorization input"
      pattern: "NormalizeSender"
    - from: "Raid Management Addon/Services/Reserves/Sync.lua"
      to: "Raid Management Addon/Localization/DiagnoseLog.en.lua"
      via: "first debug-only rejection for an already-admitted sender/kind cooldown"
      pattern: "LogSyncRequestRateLimited"
---

<objective>
Bound reserve metadata and data response work per canonical sender and request kind without changing the reserve protocol.

Purpose: Stop repeated authorized reserve requests before they construct or queue responses while preserving the normal META_REQ followed by DATA_REQ flow.
Output: Reserve-owner admission state, one shared debug diagnostic, and focused compatibility/resource-bound regressions.
</objective>

<execution_context>
@C:/Users/Massimo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Massimo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bounded-sync-requests/03-CONTEXT.md
@.planning/phases/03-bounded-sync-requests/03-RESEARCH.md
@Raid Management Addon/Services/Reserves/Sync.lua
@Raid Management Addon/Modules/Comms.lua
@Raid Management Addon/Database/DBSyncSession.lua
@Raid Management Addon/Localization/DiagnoseLog.en.lua
@tests/lua/harness/50_reserves_messaging.lua
@tests/test_sync_communications_behavior.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Specify the reserve request admission boundary before runtime changes</name>
  <files>tests/lua/harness/50_reserves_messaging.lua, tests/test_sync_communications_behavior.py</files>
  <action>Extend `installRealReservesSyncFixture` only as needed with mutable group membership keyed from the original sender, a debug enable/capture surface, controllable local-data availability, and counters around `Sync:GetPayload`, `Payload.Serialize`, direct/batch queue attempts, and emitted packets. Pre-encode inbound envelopes before resetting serialization counters. Add the dedicated Lua case `reserves_sync_incoming_requests_are_rate_limited_before_response_work` and register it as `test_reserves_incoming_requests_are_rate_limited_before_response_work` in `SyncCommunicationsBehaviorTests`; do not overload the existing outgoing-correlation or incoming-assembly cases and do not edit runtime code in this task.

Cover the complete locked matrix: unchanged first `META_REQ` response (`RMAResSync`, version 5, `META_ACK`, original request ID/target/body/priority); an independent first `DATA_REQ` at the same timestamp with unchanged chunk and `DATA_DONE` envelopes; same-kind rejection at `t + 4.999`; repeated rejection that does not move expiry; admission at exactly `t + 5.000`; `Player`, `player`, and `Player-Realm` aliasing; invalid version/target/body, invalid normalization, and non-member non-consumption; authorization re-evaluation from the raw sender with leave/re-entry not clearing the active cooldown; admitted no-data/error behavior consuming the cooldown; debug-disabled rejection with no debug/chat/warning output and exactly one debug record when enabled during an active cooldown; a service-wide maximum of 128 active sender keys across both kinds, an existing sender's other kind still admitted at capacity, sender 129 rejected without eviction, and lazy exact-boundary expiry freeing capacity. Inspect `Sync._requestAdmissions` to prove the hard map bound. For every rate/capacity rejection assert handled consumption and zero payload construction, response serialization, chunk/batch creation, or queue attempts. Commit this test-only task atomically as `test(03-01): specify reserve request admission boundary`.</action>
  <verify>Run `lua tests/lua/runtime_harness.lua reserves_sync_incoming_requests_are_rate_limited_before_response_work` and confirm the new case fails against the unmodified runtime for the missing admission behavior. Also run `python -m unittest tests.test_sync_communications_behavior.SyncCommunicationsBehaviorTests.test_reserves_incoming_requests_are_rate_limited_before_response_work`. The current shell is known to lack a Lua executable: if that remains true, record the runner absence as an environment constraint, confirm the case and Python registration exist with `rg -n "reserves_sync_incoming_requests_are_rate_limited_before_response_work|test_reserves_incoming_requests_are_rate_limited_before_response_work" tests`, and do not weaken or remove any behavior assertion.</verify>
  <done>A focused test contract exists for every reserve boundary, identity, capacity, authorization, debug-dedupe, no-work, and version-5 compatibility requirement, with runtime source still unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Admit reserve requests in the owner before response construction</name>
  <files>Raid Management Addon/Services/Reserves/Sync.lua, Raid Management Addon/Localization/DiagnoseLog.en.lua</files>
  <action>In `Services/Reserves/Sync.lua`, beside the existing request constants and transient `_incoming`/`_pendingRequests` state, add owner-local constants `REQUEST_COOLDOWN_SECONDS = 5` and `MAX_REQUEST_SENDERS = 128` plus the transient sender-keyed table `Sync._requestAdmissions`. Implement the Lua 5.1 local helper `admitResponseRequest(originalSender, requestKind)` and only the small prune/count helpers it needs to: derive a non-empty lowercase key from the existing `Comms.NormalizeSender(originalSender)` semantics; lazily remove nested request-kind states when `now - admittedAt >= 5`; remove empty sender entries; count at most 128 active senders for the whole reserve service; reject an unseen sender at capacity without eviction; preserve independent `META_REQ` and `DATA_REQ` timestamps under one sender entry; leave `admittedAt` unchanged on rejection; and store the one bounded `rejectionLogged` flag inside the admitted kind state. Invalid normalization and capacity rejection must allocate no fallback/dedupe entry.

Wire the helper into `Sync:HandleMessage` after `decodeEnvelope`, `validTarget`, raw-sender `IsGroupMember`, and each branch's exact body validation, immediately before `sendMetadata` or `sendData`. Pass `rawSource` for canonical rate-key derivation and keep that same raw value for authorization on every request; keep the existing normalized `source` only for response targeting/correlation. Return `true` for every admission rejection. Admit before the existing availability checks so a valid authorized no-data request consumes its slot, but never admit malformed or unauthorized traffic. Add only `Diag.D.LogSyncRequestRateLimited = "[Sync] Request rate limited service=%s kind=%s sender=%s"` to `DiagnoseLog.en.lua`; emit it through the established debug-only path for the first active-cooldown rejection and include only owner label, kind, and canonical sender. Do not change `PREFIX = "RMAResSync"`, `WIRE_VERSION = 5`, codecs, request IDs, targets, bodies, priorities, outbound requester throttling, ChatThrottleLib, SavedVariables, TOC, or any timer/`OnUpdate`, and do not extract a shared rate-limit module. Commit atomically as `fix(03-01): bound reserve response requests`.</action>
  <verify>Run the focused Lua case and Python method from Task 1; in a Lua-capable environment they must pass. In the current no-Lua environment, retain the explicit runner limitation and still run `python .agents/skills/wow-addon-dev-wotlk-v335a/scripts/lint_lua51.py "Raid Management Addon"`, `python .agents/skills/wow-addon-dev-wotlk-v335a/scripts/scan_xpcall.py "Raid Management Addon"`, `rg -n "C_Timer|OnUpdate|ScheduleTimer" "Raid Management Addon/Services/Reserves/Sync.lua"`, and `git diff --check`. The timer search must return no new scheduling mechanism.</verify>
  <done>Reserve `META_REQ` and `DATA_REQ` work is admitted once per canonical sender/kind per inclusive five-second boundary, retained in at most 128 transient sender entries, rejected before all response work, debug-deduplicated, and protocol-version-5 behavior is unchanged for admitted requests.</done>
</task>

</tasks>

<verification>
- The reserve-focused Lua case passes when a Lua 5.1-compatible executable is available; the known missing executable is reported as an environment constraint, never as permission to weaken COMM-04 coverage.
- Source inspection confirms request validation and raw-sender authorization precede admission, while admission precedes `sendMetadata` and `sendData`.
- Lua 5.1 lint, variadic-`xpcall` scan, no-new-timer search, and `git diff --check` are clean.
- No reserve prefix, protocol version, envelope field, payload shape, target, request correlation, queue priority, SavedVariable, or authorization contract changes.
</verification>

<success_criteria>
- Normal reserve metadata-then-data synchronization still succeeds immediately with unchanged R5 wire behavior.
- Repeated same-kind requests and capacity overflow create no reserve response work until the locked boundary permits admission.
- Canonical sender aliases cannot bypass the limit, and all state/debug memory stays transient and bounded.
</success_criteria>

<output>
After completion, create `.planning/phases/03-bounded-sync-requests/03-01-SUMMARY.md`.
</output>

---
phase: 07-ui-simplification-verification
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: []
autonomous: false
requirements: [QUAL-02]
must_haves:
  truths:
    - "Screen Notice is recorded as OBSERVED only if a normal raid/loot path publishes the internal event and the localized notice visibly renders and fades; otherwise it is DEFERRED or FAIL."
    - "Trade is recorded as OBSERVED only if both successful completion and an uncertain/failed verification followed by a clean retry are seen without premature or duplicate recording."
    - "Loot initialization is recorded as OBSERVED only if login or reload occurs during an active raid and normal loot-facing state remains coherent without a Lua error."
    - "The approved Phase 6 Logger and Attendance observations are cited unchanged and are never represented as newly executed Phase 7 checks."
  artifacts:
    - path: ".planning/phases/07-ui-simplification-verification/07-02-SUMMARY.md"
      provides: "Sanitized human checkpoint results with exact OBSERVED, DEFERRED, or FAIL dispositions"
    - path: ".planning/phases/06-logger-and-attendance-list-primitives/06-VERIFICATION.md"
      provides: "Previously approved Logger, Attendance, and Force Inspect live evidence"
      contains: "Human Verification Results"
  key_links:
    - from: "Raid Management Addon/Services/Raid/LootMethod.lua"
      to: "Raid Management Addon/Modules/UI/ScreenNotice.lua"
      via: "A normal boss-target loot-method path publishes Internal.ScreenNotice to the registered callback"
      pattern: "TriggerEvent(ScreenNoticeEvent"
    - from: "Raid Management Addon/Services/Master/Trade.lua"
      to: "normal manual trade completion"
      via: "Trade-owned candidate, accept, verification, uncertain, retry, and completion operations"
      pattern: "CompletePending"
    - from: "Raid Management Addon/Services/Loot/State.lua"
      to: "active-raid login or reload"
      via: "Database.EnsureLootRuntimeState calls ContextState.SyncRuntimeState during normal loot owner initialization"
      pattern: "ContextState.SyncRuntimeState"
---

<objective>
Collect only the live-client observations that automation cannot establish.

Purpose: Verify Screen Notice, Trade retry, and active-raid Loot initialization through normal WotLK 3.3.5a use while preserving the approved AUTOMATED/OBSERVED/DEFERRED evidence policy and reusing the already approved Phase 6 observations.
Output: `07-02-SUMMARY.md` with concise sanitized dispositions for the three live areas and exact citations to the prior Logger/Attendance evidence.
</objective>

<execution_context>
@C:/Users/Massimo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Massimo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-ui-simplification-verification/07-CONTEXT.md
@.planning/phases/07-ui-simplification-verification/07-01-SUMMARY.md
@.planning/phases/05-runtime-surface-cleanup/05-VERIFICATION.md
@.planning/phases/06-logger-and-attendance-list-primitives/06-VERIFICATION.md
@AGENTS.md
@Raid Management Addon/Services/Raid/LootMethod.lua
@Raid Management Addon/Modules/UI/ScreenNotice.lua
@Raid Management Addon/Services/Master/Trade.lua
@Raid Management Addon/Services/Loot/State.lua
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Observe Screen Notice, Trade retry, and Loot initialization in client</name>
  <files>None (human checkpoint; the output step creates 07-02-SUMMARY.md)</files>
  <action>Pause execution for the live-client procedure below. Accept only the user's explicit sanitized dispositions; do not perform synthetic callback invocations, infer unreported behavior, or convert an unavailable scenario to PASS. If the user reports FAIL, keep the checkpoint blocked and capture the exact observable regression for diagnosis. If the user reports DEFERRED because the required scenario is unavailable, preserve it as unexecuted, unpassed residual risk and allow continuation only under the locked accepted-risk policy.</action>
  <what-built>Plan 07-01 has completed the fixed-range 512-test and WotLK static acceptance gate. This checkpoint observes only the three Phase 5 behaviors that require a real WotLK 3.3.5a client; it does not repeat the approved Phase 6 Logger/Attendance smoke tests.</what-built>
  <how-to-verify>
Use a WotLK 3.3.5a client with the current addon tree. Record only sanitized outcomes; do not attach WTF files, SavedVariables contents, full taint/error logs, account identifiers, character names, loot contents, or private server data.

1. Screen Notice through the production event path:
   - Join an active raid as raid leader, start on group loot, and enable the existing automatic master-loot-on-boss-target option.
   - Target a living recognized boss through normal play. Do not invoke a debug helper or call the Screen Notice callback directly.
   - PASS only if the normal `LootMethod` path changes to master loot and a localized center notice visibly appears, sizes normally, and fades without a Lua error.
   - If the required raid/boss scenario is unavailable, report `DEFERRED`; never substitute a synthetic invocation or automation result.

2. Trade completion plus failed/uncertain verification and retry:
   - Use the normal RMA manual-loot trade workflow with an eligible recorded candidate and selected reason.
   - Complete one ordinary successful trade and confirm the award completes once with no duplicate record or Lua error.
   - In a separate attempt, exercise a genuine uncertain or failed verification path after the candidate is active, then confirm the attempt is not recorded prematurely. Retry the same workflow normally and confirm the successful retry completes once.
   - PASS only if both the ordinary success and the failed/uncertain-then-successful-retry sequence are observed. If a reliable failure/uncertainty scenario or suitable trade partner/item is unavailable, report this Trade check `DEFERRED`; a success-only observation is not sufficient.

3. Loot initialization during an active raid:
   - While a raid is active, perform login or `/reload` with RMA enabled.
   - After initialization, open the normal loot-facing UI (for example `/rma ml` and the existing loot counter/history entrypoints used in that raid) and exercise a harmless normal interaction.
   - PASS only if the active-raid loot context and visible state are coherent, frames initialize normally, values/selections are usable rather than nil/corrupt, and no Lua error occurs. Do not infer unobserved SavedVariables disk behavior.
   - If an active-raid login/reload cannot be performed, report `DEFERRED`.

4. Evidence boundary:
   - Do not repeat Logger or Attendance unless a new failure or relevant code change appears. Cite the approved Phase 6 observations exactly: Logger presentation/interaction passed; Attendance/Force Inspect passed, and `Pending` was immediate chat feedback while the row updated.
   - Return each new area as `OBSERVED PASS`, `DEFERRED`, or `FAIL`, with one concise sanitized note. Any attributable FAIL blocks Phase 7 and must be diagnosed before Plan 07-03; it is never converted to DEFERRED.
  </how-to-verify>
  <resume-signal>Reply with `Screen Notice: OBSERVED PASS|DEFERRED|FAIL`, `Trade: OBSERVED PASS|DEFERRED|FAIL`, and `Loot initialization: OBSERVED PASS|DEFERRED|FAIL`, followed by concise sanitized notes. If all executable checks pass and unavailable scenarios are explicitly deferred, type `approved` as well.</resume-signal>
  <verify>Require one explicit disposition for each of Screen Notice, Trade, and Loot initialization. Screen Notice PASS must name the normal boss-target path; Trade PASS must include success plus failed/uncertain verification and retry with no premature/duplicate record; Loot PASS must include active-raid login or reload, coherent loot-facing state, and no Lua error. Require Phase 6 Logger/Attendance evidence to remain cited as prior observation. Any FAIL keeps the gate blocked; any unavailable scenario remains DEFERRED and unpassed.</verify>
  <done>The checkpoint has three explicit sanitized dispositions, no known regression is hidden or downgraded, prior Phase 6 observations retain their source, and every unavailable live scenario remains visible as DEFERRED residual risk.</done>
</task>

</tasks>

<verification>
- Screen Notice PASS is accepted only from the normal boss-target loot-method event path, never a synthetic callback invocation.
- Trade PASS includes both ordinary completion and a failed/uncertain verification that does not record prematurely before a successful retry.
- Loot initialization PASS includes active-raid login or reload, coherent loot-facing UI/state, and no Lua error.
- Any unavailable scenario is DEFERRED with residual risk; any observed regression is FAIL and blocks the phase.
- Phase 6 Logger/Attendance results are cited from `06-VERIFICATION.md` and are not relabeled as new observations.
</verification>

<success_criteria>
- Every requested live area has an honest OBSERVED, DEFERRED, or FAIL disposition with concise sanitized evidence.
- No automation claims a human observation and no prior Phase 6 result is represented as newly executed.
- Plan 07-03 proceeds only when no known regression remains; DEFERRED items remain explicit residual risks rather than PASS.
</success_criteria>

<output>
After checkpoint completion, create `.planning/phases/07-ui-simplification-verification/07-02-SUMMARY.md`. Preserve the user's exact dispositions and concise sanitized notes, cite the Phase 6 Logger/Attendance evidence as prior approved observation, list every DEFERRED item as unexecuted/unpassed residual risk, and do not create a passing summary if any FAIL remains unresolved.
</output>

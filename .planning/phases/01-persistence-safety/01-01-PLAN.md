---
phase: 01-persistence-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Raid Management Addon/Database/SavedVariables.lua
  - tests/lua/harness/30_raid_runtime.lua
  - tests/test_raid_replication_behavior.py
autonomous: true
requirements:
  - PERS-01
  - PERS-03
must_haves:
  truths:
    - "A nil RMA_Raids value initializes a canonical format-1 archive."
    - "Every existing non-nil unsupported value reaches validation unchanged and is quarantined rather than replaced."
    - "Getter access and save preparation cannot mutate a quarantined archive."
  artifacts:
    - path: "Raid Management Addon/Database/SavedVariables.lua"
      provides: "Nil-only raid archive creation and stable quarantine classification"
      contains: "function SavedVariables.NormalizeAfterLoad"
    - path: "tests/lua/harness/30_raid_runtime.lua"
      provides: "Regression cases for nil, valid format-1, non-table, malformed format-1, and future-format archives"
      contains: "raid_archive_invalid_load_quarantine"
    - path: "tests/test_raid_replication_behavior.py"
      provides: "Python registration of the focused Lua archive cases"
  key_links:
    - from: "Raid Management Addon/Database/SavedVariables.lua"
      to: "Raid Management Addon/Database/DBRaidStore.lua"
      via: "GetRaidArchiveError remains the store fail-closed boundary"
      pattern: "GetRaidArchiveError"
    - from: "tests/test_raid_replication_behavior.py"
      to: "tests/lua/harness/30_raid_runtime.lua"
      via: "run_lua_case registrations"
      pattern: "raid_archive_"
---

<objective>
Make the `RMA_Raids` initialization contract non-destructive for every existing unsupported value.

Purpose: Eliminate the critical data-loss path before bootstrap and UI behavior are layered on top.
Output: Stable archive classification, nil-only creation, and focused regression coverage.
</objective>

<execution_context>
@C:/Users/Massimo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Massimo/.codex/get-shit-done/templates/summary.md
@C:/Users/Massimo/.codex/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-persistence-safety/01-CONTEXT.md
@.planning/phases/01-persistence-safety/01-RESEARCH.md
@Raid Management Addon/Database/SavedVariables.lua
@Raid Management Addon/Database/DBRaidStore.lua
@tests/lua/harness/30_raid_runtime.lua
@tests/test_raid_replication_behavior.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing preservation regressions</name>
  <files>tests/lua/harness/30_raid_runtime.lua, tests/test_raid_replication_behavior.py</files>
  <action>Add focused cases that install each archive variant before the SavedVariables owner can normalize it: nil, a pre-existing valid format-1 archive, a scalar/non-table value, a table with `formatVersion` greater than 1, a table with an unsupported older format, and an invalid format-1 archive. Assert nil alone creates the canonical archive. For the valid format-1 fixture, assert `NormalizeAfterLoad()` and `PrepareForSave()` both succeed while preserving archive identity and deep value. For every non-nil invalid value, assert reference/scalar identity and deep value remain unchanged after module initialization, `EnsureAll()`, `GetRaids()`, `NormalizeAfterLoad()`, and `PrepareForSave()`. Assert stable category/reason exposure and fail-closed save preparation. Register the new cases in the existing Python wrapper. Run the cases before implementation and confirm they fail for the demonstrated replacement path; do not weaken current valid or malformed format-1 assertions.</action>
  <verify>Run `python -m unittest tests.test_raid_replication_behavior.RaidReplicationBehaviorTests -v`. If the environment lacks `lua`, record that runner limitation and still run `python -m unittest tests.test_raid_replication_behavior -v` to confirm discovery reaches only the documented runner gate.</verify>
  <done>Tests distinguish fresh nil initialization, a valid format-1 load/save-preparation path, and all existing invalid values; they demonstrate that the current implementation replaces at least the unsupported cases before the fix.</done>
</task>

<task type="auto">
  <name>Task 2: Implement nil-only creation and stable quarantine classification</name>
  <files>Raid Management Addon/Database/SavedVariables.lua</files>
  <action>Refactor the private archive helper so it creates `newRaidArchive()` only when `_G.RMA_Raids == nil`. Preserve every existing non-nil value exactly. Classify invalid type before accessing fields; classify unsupported/future format before invoking the current-format validator; use the existing validator for format-1 structural corruption. Store stable non-localized reason/category codes in the existing SavedVariables owner and keep `GetRaidArchiveError()` compatible with `DBRaidStore`'s guard. Ensure `EnsureAll()`, `GetRaids()`, `NormalizeAfterLoad()`, module-load initialization, and `PrepareForSave()` cannot replace or normalize quarantined data. Do not add migrations, shadow archives, repair behavior, new SavedVariables, or direct persistence access outside this owner.</action>
  <verify>Run the focused archive tests from Task 1, then `python .agents/skills/wow-addon-dev-wotlk-v335a/scripts/lint_lua51.py "Raid Management Addon"` and `python .agents/skills/wow-addon-dev-wotlk-v335a/scripts/scan_xpcall.py "Raid Management Addon"` using the bundled Python path if `python` is unavailable.</verify>
  <done>Nil creates the canonical archive; unsupported existing values remain identity/deep-equal unchanged through load, access, and save preparation; the store receives a stable quarantine reason.</done>
</task>

</tasks>

<verification>
- Inspect `git diff` to confirm only the SavedVariables owner and focused tests changed.
- Re-run the valid and malformed format-1 cases, the quarantined-read cases, and all new variants.
- Confirm no non-RMA SavedVariable, migration, repair path, or vendored file was introduced.
</verification>

<success_criteria>
- PERS-01 is satisfied for nil, scalar, unsupported/future, and malformed format-1 inputs.
- PERS-03 has regression coverage for a valid format-1 load/save-preparation path, invalid archive preservation, and save rejection.
- Lua 5.1 validation remains clean.
</success_criteria>

<output>
After completion, create `.planning/phases/01-persistence-safety/01-01-SUMMARY.md`.
</output>

---
phase: 01-persistence-safety
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - Raid Management Addon/Init.lua
  - Raid Management Addon/Database/SavedVariables.lua
  - Raid Management Addon/Localization/DiagnoseLog.en.lua
  - Raid Management Addon/Localization/localization.en.lua
  - Raid Management Addon/Localization/localization.ru.lua
  - Raid Management Addon/Localization/localization.zhCN.lua
  - Raid Management Addon/Localization/localization.es.lua
  - Raid Management Addon/Localization/localization.fr.lua
  - tests/lua/harness/30_raid_runtime.lua
  - tests/test_runtime_foundations_behavior.py
  - tests/test_localization_contract.py
autonomous: true
requirements:
  - PERS-02
  - PERS-03
must_haves:
  truths:
    - "Quarantine is a successful degraded initialization, not a global addon startup failure."
    - "The user receives one localized non-blocking warning per login/reload."
    - "A subsequent load with a valid archive clears transient quarantine and resumes normally."
  artifacts:
    - path: "Raid Management Addon/Init.lua"
      provides: "Normalization-result handling and once-per-session warning"
      contains: "SavedVariables.NormalizeAfterLoad"
    - path: "Raid Management Addon/Localization/localization.en.lua"
      provides: "Canonical quarantine user-facing strings"
    - path: "tests/lua/harness/30_raid_runtime.lua"
      provides: "Degraded bootstrap and recovery regressions"
  key_links:
    - from: "Raid Management Addon/Init.lua"
      to: "Raid Management Addon/Database/SavedVariables.lua"
      via: "ADDON_LOADED consumes normalization result and category"
      pattern: "NormalizeAfterLoad"
    - from: "Raid Management Addon/Init.lua"
      to: "Raid Management Addon/Localization/localization.en.lua"
      via: "addon:warn uses localized quarantine format"
      pattern: "addon:warn"
---

<objective>
Turn archive rejection into an explicit, localized, recoverable degraded bootstrap state.

Purpose: Keep unrelated RMA features usable while making the quarantined history condition visible and stable.
Output: Transient bootstrap state, one warning, localized diagnostics, and recovery tests.
</objective>

<execution_context>
@C:/Users/Massimo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Massimo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-persistence-safety/01-CONTEXT.md
@.planning/phases/01-persistence-safety/01-RESEARCH.md
@.planning/phases/01-persistence-safety/01-01-SUMMARY.md
@Raid Management Addon/Init.lua
@Raid Management Addon/Database/SavedVariables.lua
@tests/lua/harness/30_raid_runtime.lua
@tests/test_localization_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Specify degraded bootstrap and localized diagnostic contracts</name>
  <files>tests/lua/harness/30_raid_runtime.lua, tests/test_runtime_foundations_behavior.py, tests/test_localization_contract.py</files>
  <action>Add regression cases where `NormalizeAfterLoad()` returns each stable quarantine category. Assert `ADDON_LOADED` completes unrelated initialization, sets `State.initialized`, stores only transient quarantine status, emits exactly one warning, and does not repeat it for blocked history operations. Simulate a new load with a valid archive and assert the quarantine state clears automatically. Add localization contract expectations for the user warning, read-only/unavailable history label, and quarantined raid-sync status across enUS, ruRU, zhCN, esES, and frFR. Assert messages never interpolate archive contents or player data. Do not turn quarantine into an exception-based bootstrap failure.</action>
  <verify>Run `python -m unittest tests.test_runtime_foundations_behavior tests.test_localization_contract -v`; Lua-backed cases may stop only at the documented missing-runner gate.</verify>
  <done>Tests define successful degraded initialization, warning cardinality, safe diagnostic content, locale parity, and automatic recovery on a valid later load.</done>
</task>

<task type="auto">
  <name>Task 2: Wire transient quarantine and localized warning into ADDON_LOADED</name>
  <files>Raid Management Addon/Init.lua, Raid Management Addon/Database/SavedVariables.lua, Raid Management Addon/Localization/DiagnoseLog.en.lua, Raid Management Addon/Localization/localization.en.lua, Raid Management Addon/Localization/localization.ru.lua, Raid Management Addon/Localization/localization.zhCN.lua, Raid Management Addon/Localization/localization.es.lua, Raid Management Addon/Localization/localization.fr.lua</files>
  <action>Consume both values returned by `SavedVariables.NormalizeAfterLoad()` before committing `State.initialized`. On rejection, keep initialization successful, expose a transient session-only quarantine category/state, write validator detail only through `addon.Diag` debug output, and emit one `addon:warn` message for that load. Add concise translations for all supported locales covering unsupported/future format, corrupt format-1 structure, and invalid SavedVariable type; include only stable code/version metadata and backup/restore plus `/reload` guidance. Add the already-decided read-only history and suspended-sync labels for Plan 03. On a valid load clear transient quarantine state without persisting any flag. Do not add popup UI, repeated warning hooks, new SavedVariables, or English inline fallbacks.</action>
  <verify>Run the Task 1 tests, `python .agents/skills/wow-addon-dev-wotlk-v335a/scripts/lint_lua51.py "Raid Management Addon"`, and `rg -n 'RMA_Raids|quarant' "Raid Management Addon/Localization" "Raid Management Addon/Init.lua"` to inspect data-safe wording and ownership.</verify>
  <done>Invalid history yields one localized warning while the addon completes initialization; valid history clears the state; all five locale catalogs satisfy parity.</done>
</task>

</tasks>

<verification>
- Confirm warning count is exactly one per fixture load and no popup is defined.
- Confirm quarantine state is absent from every SavedVariable and recreated solely from validation.
- Confirm unrelated initialization callbacks still run in the degraded fixture.
</verification>

<success_criteria>
- PERS-02's degraded bootstrap and localized diagnostic behavior are implemented.
- PERS-03 covers invalid categories, warning cardinality, and automatic recovery.
- Localization contracts pass for every supported locale.
</success_criteria>

<output>
After completion, create `.planning/phases/01-persistence-safety/01-02-SUMMARY.md`.
</output>

---
phase: 01-persistence-safety
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - Raid Management Addon/Controllers/Logger.lua
  - Raid Management Addon/Database/DBSyncer.lua
  - tests/lua/harness/60_loot_ui.lua
  - tests/lua/harness/70_raid_sync.lua
  - tests/test_raid_replication_behavior.py
  - tests/test_sync_communications_behavior.py
autonomous: true
requirements:
  - PERS-02
  - PERS-03
must_haves:
  truths:
    - "Quarantined raid history is visibly unavailable and read-only rather than looking empty."
    - "Raid-history replication, offers, imports, and mutation work are suspended while quarantined."
    - "Reserve and loot-distribution synchronization remain unaffected."
  artifacts:
    - path: "Raid Management Addon/Controllers/Logger.lua"
      provides: "Explicit read-only quarantine presentation for raid history"
    - path: "Raid Management Addon/Database/DBSyncer.lua"
      provides: "Quarantine admission guard for raid-history synchronization"
    - path: "tests/lua/harness/70_raid_sync.lua"
      provides: "Raid-sync suspension and no-peer-recovery regressions"
  key_links:
    - from: "Raid Management Addon/Controllers/Logger.lua"
      to: "Raid Management Addon/Database/SavedVariables.lua"
      via: "existing database owner exposes transient quarantine reason/category"
      pattern: "GetRaidArchiveError"
    - from: "Raid Management Addon/Database/DBSyncer.lua"
      to: "Raid Management Addon/Database/DBRaidStore.lua"
      via: "sync admission stops before archive import or mutation"
      pattern: "GetRaidStore"
---

<objective>
Make raid-history consumers visibly and operationally fail closed while leaving unrelated synchronization usable.

Purpose: Complete the user-decided degraded mode at the UI and raid-sync boundaries.
Output: Read-only history state, suspended raid replication/import, and integration regressions.
</objective>

<execution_context>
@C:/Users/Massimo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Massimo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-persistence-safety/01-CONTEXT.md
@.planning/phases/01-persistence-safety/01-RESEARCH.md
@.planning/phases/01-persistence-safety/01-02-SUMMARY.md
@Raid Management Addon/Controllers/Logger.lua
@Raid Management Addon/Database/DBSyncer.lua
@tests/lua/harness/60_loot_ui.lua
@tests/lua/harness/70_raid_sync.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Present quarantined history as explicitly read-only</name>
  <files>Raid Management Addon/Controllers/Logger.lua, tests/lua/harness/60_loot_ui.lua, tests/test_raid_replication_behavior.py</files>
  <action>Extend the logger/history fixture to assert that quarantine is distinguishable from a legitimate empty history. Reuse the smallest existing status/enablement pattern: show the localized unavailable/quarantine label, clear selection safely, disable all history mutation/share/maintenance actions, and leave the window open for read-only status visibility. Confirm unrelated QuickBar/general UI paths still initialize. Do not add a new screen, popup, destructive reset action, or duplicate persistence checks in widgets/services.</action>
  <verify>Run `python -m unittest tests.test_raid_replication_behavior -v` plus the focused Lua UI case through the existing harness when `lua` is available.</verify>
  <done>The history window explicitly reports quarantine and exposes no enabled mutation, share, import, or maintenance action while unrelated UI remains usable.</done>
</task>

<task type="auto">
  <name>Task 2: Suspend only raid-history synchronization during quarantine</name>
  <files>Raid Management Addon/Database/DBSyncer.lua, tests/lua/harness/70_raid_sync.lua, tests/test_sync_communications_behavior.py</files>
  <action>Add sync regressions that place the raid archive in quarantine and attempt live replication, historical offer/import, snapshot/range recovery, and peer handover. Assert the syncer reports the localized quarantined/suspended state and rejects before serialization, queue allocation, import, or store mutation; a peer must never replace the archive. Implement one centralized quarantine admission check in the raid syncer using the existing SavedVariables/store error owner. Do not change `RMARaidSync`, protocol version 5, message shapes, or generic Comms. Explicitly assert Reserves and Distribution handlers remain callable/unaffected because they do not use the raid-history syncer.</action>
  <verify>Run `python -m unittest tests.test_sync_communications_behavior -v`, then the TOC, Lua 5.1, and `xpcall` validators documented by the project skill.</verify>
  <done>All raid-history synchronization and import paths fail closed before response/mutation work; reserve and distribution synchronization contracts are unchanged.</done>
</task>

</tasks>

<verification>
- Run focused UI and raid-sync cases, then the complete available unittest discovery.
- Run TOC, Lua 5.1, variadic-`xpcall`, XML handler, and branding scans.
- Inspect the final diff for any wire-format, non-RMA SavedVariable, vendored library, or unrelated feature changes.
</verification>

<success_criteria>
- Quarantined history is visible and read-only rather than silently empty.
- Raid sync/import is suspended without peer overwrite or queued work.
- Reserves, Distribution, and unrelated addon initialization remain operational.
- PERS-02 and PERS-03 are fully covered at consumer boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/01-persistence-safety/01-03-SUMMARY.md`.
</output>

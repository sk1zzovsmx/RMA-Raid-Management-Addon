---
phase: 06-logger-and-attendance-list-primitives
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - Raid Management Addon/Controllers/Logger.lua
  - tests/lua/harness/70_raid_sync.lua
  - tests/test_sync_communications_behavior.py
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "Logger raid and loot lists retain exact calculated header/row widths, offsets, item-icon header allowance, and Source text/hit-box width parity through shared primitives."
    - "Logger sort headers bind once to Logger-owned sort keys, while Logger alone still disables Source sorting and changes Source-header alpha when a boss is selected."
    - "Logger count/context titles and selected empty-state text remain byte-for-byte equivalent, including the explicit fallback-context behavior."
    - "Logger keeps its layout constants, raid/loot descriptor tables, sorters, Source rendering/tooltips/interactions, localization, refresh timing, and controller orchestration."
  artifacts:
    - path: "Raid Management Addon/Controllers/Logger.lua"
      provides: "Logger integration with the focused shared list primitives while retaining feature presentation ownership"
      contains: "local LOOT_LAYOUT_COLUMNS"
    - path: "tests/lua/harness/70_raid_sync.lua"
      provides: "Focused Logger geometry, Source availability, title, empty-state, and binding regression"
      contains: "logger_lists_use_shared_layout_primitives_without_behavior_drift"
    - path: "tests/test_sync_communications_behavior.py"
      provides: "Focused Python registration for the Logger Lua regression"
      contains: "test_logger_lists_use_shared_layout_primitives_without_behavior_drift"
  key_links:
    - from: "Raid Management Addon/Controllers/Logger.lua"
      to: "Raid Management Addon/Modules/UI/ListController.lua"
      via: "Logger passes its local constants, width tables, descriptors, and explicit geometry to the focused Lists operations"
      pattern: "UI.Lists"
    - from: "Raid Management Addon/Controllers/Logger.lua"
      to: "RMALootHistoryLootHeaderSource"
      via: "Logger-owned updateSourceHeaderState retains mouse and alpha availability rules independently of shared initial sort binding"
      pattern: "updateSourceHeaderState"
---

<objective>
Replace Logger's duplicated list mechanics with the tested shared operations without moving or changing Logger presentation policy.

Purpose: Prove the shared core handles mechanics while Logger retains every feature-specific decision, especially item allowance, Source interaction bounds, contextual sort availability, titles, and empty states.
Output: A mechanically smaller Logger controller plus focused behavior regression.
</objective>

<execution_context>
@C:/Users/Massimo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Massimo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-logger-and-attendance-list-primitives/06-CONTEXT.md
@.planning/phases/06-logger-and-attendance-list-primitives/06-01-SUMMARY.md
@AGENTS.md
@Raid Management Addon/Modules/UI/ListController.lua
@Raid Management Addon/Controllers/Logger.lua
@tests/lua/harness/70_raid_sync.lua
@tests/test_sync_communications_behavior.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED - capture Logger layout and Source behavior at the controller edge</name>
  <files>tests/lua/harness/70_raid_sync.lua, tests/test_sync_communications_behavior.py</files>
  <action>Add `cases.logger_lists_use_shared_layout_primitives_without_behavior_drift` by extending the existing `installLoggerShareFixture`; register one matching method in `SyncCommunicationsBehaviorTests`. Instrument the seven real/shared `UI.Lists` operations with narrow spies that preserve their behavior and capture calls, and extend fake controls to record width, anchor, mouse-enabled state, alpha, text, visibility, and scripts. Do not duplicate the production width algorithm inside this controller test: use the Phase 6 shared implementation and assert the resulting observable geometry.

Exercise raid and loot `localize`, representative raid/loot `drawRow`, and `postUpdate` paths at both a valid ScrollFrame width and invalid ScrollFrame/frame fallback. Assert exact numeric results already protected by Plan 01, including 240 fallback/clamp, explicit Logger 24 gutter, 3/34 offsets, 6 gaps, fixed 30 icon allowance, item header extra width, Source text width equal to `SourceHitBox` width, header/row parity, and unchanged call points during localization, row drawing, and post-update. Invoke localization twice and prove each sort header's OnClick is installed once; click representative headers and prove Logger-owned controller sort keys are received.

Set and clear `module.selectedBoss`, invoke the existing update path, and assert the Source header changes from mouse enabled/alpha 1 to mouse disabled/alpha 0.6 and back without the shared binder making that decision. Assert Source text/tooltip hit-box ownership remains on the row and that shared row layout only applies its width. Cover empty and populated raid/loot `postUpdate` titles and hints, including exact `Title (n)`, exact `Title (n) - Context`, and Logger's explicit fallback context/empty-hint selection. Add source-ownership assertions in the Python method: `LoggerLayout`, `RAID_LAYOUT_COLUMNS`, `LOOT_LAYOUT_COLUMNS`, `updateSourceHeaderState`, Source tooltip/render code, and Logger empty-state selectors remain in `Logger.lua`, while none move to `ListController.lua`.

Run RED against Plan 01 plus the unchanged Logger controller and require failure because the shared-operation spies report that Logger still executes its local duplicated helpers. The failure must occur after the fixture loads and must not be a numeric mismatch or missing widget. Commit the failing regression only as `test(06-02): lock Logger list presentation parity`.</action>
  <verify>In PowerShell, create/preflight the external Lua shim before RED: `$shimDir = Join-Path ([System.IO.Path]::GetTempPath()) 'rma-phase06-lua'; New-Item -ItemType Directory -Force -Path $shimDir | Out-Null; Copy-Item -LiteralPath 'C:\tools\LuaJIT\bin\luajit.exe' -Destination (Join-Path $shimDir 'lua.exe') -Force; Copy-Item -LiteralPath 'C:\tools\LuaJIT\bin\lua51.dll' -Destination (Join-Path $shimDir 'lua51.dll') -Force; $env:PATH = "$shimDir;$env:PATH"; $pythonExe = 'C:\Users\Massimo\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe'; &amp; $pythonExe -c "import shutil; assert shutil.which('lua')"; &amp; $pythonExe -m unittest tests.test_sync_communications_behavior.SyncCommunicationsBehaviorTests.test_logger_lists_use_shared_layout_primitives_without_behavior_drift`. Confirm `lua.exe` and `lua51.dll` are side by side outside the repo, LuaJIT reaches the controller, and RED fails specifically because expected shared primitive call counts are zero/absent on unchanged Logger.</verify>
  <done>A committed focused Logger regression fails for the intended unextracted controller mechanics while proving the fixture can observe exact geometry, binding, Source state, title, and empty-state behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Route Logger mechanics through shared primitives</name>
  <files>Raid Management Addon/Controllers/Logger.lua</files>
  <action>At Logger's existing top-level UI bindings, assert/cache the seven focused `UI.Lists` operations created by Plan 01. Replace only the duplicated helper bodies/calls for content width, budget arithmetic, header positioning/widths, row widths, sort-header binding, count/context formatting, and label text/visibility. Pass every Logger value explicitly: fallback 240, gutter 24, row/header gap 6, current start/top offsets, raid/loot lead offsets and gap counts, returned icon width 30, the local `RAID_LAYOUT_COLUMNS`/`LOOT_LAYOUT_COLUMNS`, and the existing explicit bound flag.

Keep `LoggerLayout`, both minimum/ratio maps, both descriptor tables (including the deliberately duplicated raid descriptors), `headerExtraWidthKey = "icon"`, `hitBoxKey = "SourceHitBox"`, sort keys/sorters, localization selection, `getRaidContextLabel`, `getLootPanelContextLabel`, `getLootEmptyStateText`, row drawing, Source text/tooltip interaction, `updateSourceHeaderState`, and all refresh call sites in Logger. Shared binding must occur only at the same initial localization points; do not move or weaken boss-selected Source mouse/alpha logic. Shared title formatting receives already selected localized base/context/fallback text, and shared label application receives already selected empty-state text. Do not change XML, strings, frame names, feature behavior, or introduce wrapper aliases around the shared functions.

Delete only the now-redundant local mechanical helpers. Re-run the focused regression to GREEN and commit atomically as `refactor(06-02): share Logger list mechanics`.</action>
  <verify>In PowerShell, recreate/preflight the external shim before GREEN: `$shimDir = Join-Path ([System.IO.Path]::GetTempPath()) 'rma-phase06-lua'; New-Item -ItemType Directory -Force -Path $shimDir | Out-Null; Copy-Item -LiteralPath 'C:\tools\LuaJIT\bin\luajit.exe' -Destination (Join-Path $shimDir 'lua.exe') -Force; Copy-Item -LiteralPath 'C:\tools\LuaJIT\bin\lua51.dll' -Destination (Join-Path $shimDir 'lua51.dll') -Force; $env:PATH = "$shimDir;$env:PATH"; $pythonExe = 'C:\Users\Massimo\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe'; &amp; $pythonExe -c "import shutil; assert shutil.which('lua')"; &amp; $pythonExe -m unittest tests.test_sync_communications_behavior.SyncCommunicationsBehaviorTests.test_logger_lists_use_shared_layout_primitives_without_behavior_drift` and require PASS. Confirm `lua.exe` and `lua51.dll` remain side by side outside the repo. Use `rg -n "LoggerLayout|RAID_LAYOUT_COLUMNS|LOOT_LAYOUT_COLUMNS|headerExtraWidthKey|hitBoxKey|updateSourceHeaderState|getLootEmptyStateText|buildSourceTooltipModel" "Raid Management Addon/Controllers/Logger.lua"` to confirm feature ownership remains. Use `rg -n "LOGGER_LIST_WIDTH_FALLBACK|LOGGER_SCROLLBAR_GUTTER_WIDTH|LOGGER_ROW_COLUMN_GAP|LOGGER_HEADER_COLUMN_GAP" "Raid Management Addon/Modules/UI/ListController.lua"`; require no matches because visual constants remain controller-owned. Run `git diff --check`.</verify>
  <done>Logger is GREEN through shared mechanics with exact geometry, one-time sort binding, Source availability/hit-box behavior, titles, empty states, and feature ownership unchanged.</done>
</task>

</tasks>

<verification>
- The focused Logger regression fails before and passes after the controller extraction for the expected reason.
- Logger still recalculates at localization, row drawing, and post-update only; no resize listener, timer, cache, polling, or new refresh point exists.
- Source sorting availability, alpha, text, tooltip, and hit-box behavior remain Logger-owned; the shared layer performs only stable initial binding and supplied width application.
- Do not run complete unittest discovery in this plan; it remains reserved for 06-03 after Attendance is also migrated.
</verification>

<success_criteria>
- UI-02 is protected by exact Logger controller-edge regression.
- Logger consumes the shared width/header/row/sort/title/label operations without surrendering descriptors, constants, sorting rules, localized selections, or feature rendering.
- Focused LuaJIT test and `git diff --check` pass.
</success_criteria>

<output>
After completion, create `.planning/phases/06-logger-and-attendance-list-primitives/06-02-SUMMARY.md`, then advance `.planning/STATE.md`, record decisions/metrics/session continuity, and update `.planning/ROADMAP.md` through the standard execute-plan workflow.
</output>

---
phase: 02-locale-independent-raid-recognition
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - Raid Management Addon/Init.lua
  - Raid Management Addon/Services/Raid/Session.lua
  - Raid Management Addon/Localization/DiagnoseLog.en.lua
  - Raid Management Addon/Localization/localization.en.lua
  - Raid Management Addon/Localization/localization.ru.lua
  - Raid Management Addon/Localization/localization.zhCN.lua
  - Raid Management Addon/Localization/localization.es.lua
  - Raid Management Addon/Localization/localization.fr.lua
  - tests/lua/harness/30_raid_runtime.lua
  - tests/test_runtime_foundations_behavior.py
  - tests/test_localization_contract.py
autonomous: true
requirements: [RAID-02, RAID-03, LOCL-02]
must_haves:
  truths:
    - "An unknown raid entry produces one concise localized warning, while technical name and map ID appear only in debug diagnostics."
    - "Overlapping entry events and delayed checks do not repeat the normal warning for the same unknown identity."
    - "Existing bounded delayed checks can recognize initially incomplete instance data without a new zone event or per-frame polling."
  artifacts:
    - path: "Raid Management Addon/Init.lua"
      provides: "Unknown-instance dedupe and authoritative retry coordinator"
    - path: "Raid Management Addon/Services/Raid/Session.lua"
      provides: "Existing bounded timer cadence calling the Init-owned refresh path"
    - path: "Raid Management Addon/Localization/DiagnoseLog.en.lua"
      provides: "Debug-only received instance details"
    - path: "Raid Management Addon/Localization/localization.en.lua"
      provides: "English scalar warning contract"
    - path: "tests/lua/harness/30_raid_runtime.lua"
      provides: "Warning cardinality and delayed-recovery behavior"
  key_links:
    - from: "Raid Management Addon/Services/Raid/Session.lua"
      to: "Raid Management Addon/Init.lua"
      via: "narrow refresh callback that re-runs resolver and transactional dataset activation"
      pattern: "ScheduleInstanceChecks"
    - from: "Raid Management Addon/Init.lua"
      to: "Raid Management Addon/Localization/localization.en.lua"
      via: "localized non-technical warning scalar"
      pattern: "addon:warn"
---

<objective>
Make unsupported-instance behavior quiet, informative, and recoverable during the existing bounded delayed checks.

Purpose: Fail closed without warning spam, stale identity, technical player-facing text, or a new polling mechanism.
Output: Init-owned retry coordination, one-warning dedupe, debug-only technical diagnostics, and locale-complete warning strings.
</objective>

<execution_context>
@C:/Users/Massimo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Massimo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-locale-independent-raid-recognition/02-CONTEXT.md
@.planning/phases/02-locale-independent-raid-recognition/02-RESEARCH.md
@.planning/phases/02-locale-independent-raid-recognition/02-01-SUMMARY.md
@Raid Management Addon/Init.lua
@Raid Management Addon/Services/Raid/Session.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-enter canonical coordination from bounded retries and dedupe unknown warnings</name>
  <files>Raid Management Addon/Init.lua, Raid Management Addon/Services/Raid/Session.lua, Raid Management Addon/Localization/DiagnoseLog.en.lua, tests/lua/harness/30_raid_runtime.lua, tests/test_runtime_foundations_behavior.py</files>
  <action>Add behavior cases for recognized-to-unknown clearing, overlapping `RAID_INSTANCE_WELCOME`, `ZONE_CHANGED_NEW_AREA`, and `PLAYER_ENTERING_WORLD` notifications, repeated delayed callbacks, leave/re-enter warning reset, and a `GetInstanceInfo()` fixture that changes from missing/zero map ID to 631 during the existing callbacks. Then expose/pass a narrow Init-owned refresh operation to the Raid timer owner so each 0.3/0.8/1.5/2.5/3.5-second retry re-runs `GetInstanceInfo`, `ResolveInstanceKey`, and the full transactional activation path without recursively scheduling another retry batch. Track the last warned unknown token at the Init event boundary using received map ID plus display name; reset it on recognized or non-raid transition. Entry/zone events may request one warning, but retry callbacks must run in no-warning mode. Replace the technical normal warning with one `addon.L` scalar, and put received name, map ID, and difficulty only in a new `addon.Diagnose` debug template. Do not add `OnUpdate`, an unbounded timer, popup, generic raid admission, or stale-context reuse.</action>
  <verify>Run `python -m unittest tests.test_runtime_foundations_behavior`, the Lua 5.1 lint and `xpcall` scanner, and `git diff --check`. If Lua 5.1 is unavailable, record the focused behavior cases as runner-blocked rather than passed; inspect the registered callbacks and static source contracts.</verify>
  <done>Delayed checks observe changing instance data through the same canonical coordinator, unsupported transitions clear state, one normal warning is emitted per entry/change identity, and technical detail remains debug-only.</done>
</task>

<task type="auto">
  <name>Task 2: Localize the concise unknown-instance warning on every supported locale</name>
  <files>Raid Management Addon/Localization/localization.en.lua, Raid Management Addon/Localization/localization.ru.lua, Raid Management Addon/Localization/localization.zhCN.lua, Raid Management Addon/Localization/localization.es.lua, Raid Management Addon/Localization/localization.fr.lua, tests/test_localization_contract.py</files>
  <action>Add the same scalar warning key to English, Russian, Simplified Chinese, Spanish, and French catalogs using concise, non-technical wording. The normal string must not interpolate or expose the received zone name, map ID, or difficulty; those belong only to the diagnostic template from Task 1. Preserve locale-file early gates and scalar-only catalogs, and do not recreate `RaidZones` or introduce a locale library. Extend the localization contract to require the key in all five catalogs and reject technical placeholders in the player-facing string.</action>
  <verify>Run `python -m unittest tests.test_localization_contract`, `py -3 .agents/skills/wow-addon-dev-wotlk-v335a/scripts/lint_lua51.py "Raid Management Addon"`, and `git diff --check`.</verify>
  <done>All supported locales provide the warning scalar with matching non-technical semantics, while diagnostic details remain outside user-facing locale text.</done>
</task>

</tasks>

<verification>
- The existing bounded timer cadence is reused; no permanent `OnUpdate` or additional scheduler is introduced.
- Delayed callbacks re-enter Init coordination and never bypass dataset activation or create another retry batch.
- Normal warning cardinality is one per unknown entry/change identity; debug data is not user-facing.
- Locale catalog gates and scalar-only contracts remain intact.
</verification>

<success_criteria>
- Initially incomplete instance data becomes recognized automatically when a bounded retry later yields a supported map ID.
- Unknown raids remain fail closed and cannot inherit the prior canonical key.
- Repeated events/retries do not spam chat, while leaving and re-entering permits one fresh warning.
</success_criteria>

<output>
After completion, create `.planning/phases/02-locale-independent-raid-recognition/02-02-SUMMARY.md`.
</output>

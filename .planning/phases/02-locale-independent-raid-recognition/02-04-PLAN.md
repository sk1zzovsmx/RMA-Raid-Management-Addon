---
phase: 02-locale-independent-raid-recognition
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - Raid Management Addon/Localization/localization.en.lua
  - Raid Management Addon/Localization/localization.ru.lua
  - Raid Management Addon/Localization/localization.zhCN.lua
  - Raid Management Addon/Localization/localization.es.lua
  - Raid Management Addon/Localization/localization.fr.lua
  - tests/test_localization_contract.py
autonomous: true
requirements: [LOCL-01, LOCL-02]
must_haves:
  truths:
    - "All four non-English catalogs contain the exact evidence-backed value for every one of the 15 existing yell fallbacks."
    - "English and current-locale exact texts remain separately available, with one expected canonical instance per definition."
    - "Any catalog byte drift or incomplete fallback coverage fails the static localization contract."
  artifacts:
    - path: "Raid Management Addon/Localization/localization.en.lua"
      provides: "English-owned fallback definitions and exact English strings"
    - path: "Raid Management Addon/Localization/localization.ru.lua"
      provides: "Verified ruRU exact yell scalars"
    - path: "Raid Management Addon/Localization/localization.zhCN.lua"
      provides: "Verified zhCN exact yell scalars"
    - path: "Raid Management Addon/Localization/localization.es.lua"
      provides: "Verified esES exact yell scalars"
    - path: "Raid Management Addon/Localization/localization.fr.lua"
      provides: "Verified frFR exact yell scalars"
    - path: "tests/test_localization_contract.py"
      provides: "Evidence completeness and runtime-string digest enforcement"
  key_links:
    - from: ".planning/phases/02-locale-independent-raid-recognition/02-YELL-EVIDENCE.md"
      to: "Raid Management Addon/Localization/localization.*.lua"
      via: "per-key UTF-8 length and digest checked against each exact catalog value"
      pattern: "SHA"
    - from: "Raid Management Addon/Localization/localization.en.lua"
      to: "Raid Management Addon/Localization/localization.*.lua"
      via: "one scalar locale key per existing fallback definition"
      pattern: "BossYell"
---

<objective>
Bind the verified 60/60 source data to scalar locale catalogs and a compact definition list for only the existing yell fallbacks.

Purpose: Supply exact English/current-locale values and canonical instance metadata without adding encounter capability or display-name admission tables.
Output: Five catalog updates plus evidence-enforcing static tests.
</objective>

<execution_context>
@C:/Users/Massimo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Massimo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-locale-independent-raid-recognition/02-CONTEXT.md
@.planning/phases/02-locale-independent-raid-recognition/02-RESEARCH.md
@.planning/phases/02-locale-independent-raid-recognition/02-02-SUMMARY.md
@.planning/phases/02-locale-independent-raid-recognition/02-03-SUMMARY.md
@.planning/phases/02-locale-independent-raid-recognition/02-YELL-EVIDENCE.md
@Raid Management Addon/Localization/localization.en.lua
@tests/test_localization_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing evidence-bound catalog and definition contracts</name>
  <files>tests/test_localization_contract.py</files>
  <action>Parse the accepted 60/60 evidence matrix and add static tests requiring all 15 stable scalar keys in ruRU, zhCN, esES, and frFR. Recompute each decoded runtime string's UTF-8 byte length and SHA-256 digest and compare it to the corresponding matrix row. Require exactly 15 English-owned definitions with the existing boss label, stable scalar locale key, exact English text, and expected canonical instance (`naxxramas`, `ulduar`, `trial of the crusader`, `icecrown citadel`, or `the ruby sanctum` per research). Enforce scalar-only locale overrides and reject copied `RaidZones` or per-locale `BossYells` tables. The test setup must fail immediately unless the matrix is complete and clean.</action>
  <verify>Run `python -m unittest tests.test_localization_contract` and confirm the new cases fail only because catalogs/definitions are not yet updated, never because the matrix parser accepts incomplete evidence.</verify>
  <done>Static contracts bind every planned runtime locale value and definition to the accepted matrix and reject missing keys, byte drift, wrong scope, extra definitions, and display-name admission tables.</done>
</task>

<task type="auto">
  <name>Task 2: Add exact locale scalars and existing-fallback metadata</name>
  <files>Raid Management Addon/Localization/localization.en.lua, Raid Management Addon/Localization/localization.ru.lua, Raid Management Addon/Localization/localization.zhCN.lua, Raid Management Addon/Localization/localization.es.lua, Raid Management Addon/Localization/localization.fr.lua</files>
  <action>Proceed only after independently confirming Plan 02-03's matrix is 60/60. Convert only the current 15 English `BossYells` strings into a compact English-owned definition list/table containing existing boss label, stable scalar locale key, exact English text, and expected canonical instance. Add each exact evidence-derived scalar to ruRU, zhCN, esES, and frFR without changing case, whitespace, punctuation, apostrophes, ellipses, or Unicode bytes. Do not add the commented Steelbreaker line or any new encounter. Preserve English-first TOC behavior, early locale gates, and scalar-only override files. Do not add a module, dependency, display-name gate, copied per-locale table, machine translation, or unsourced literal.</action>
  <verify>Run `python -m unittest tests.test_localization_contract`; it must validate all 60 catalog values against the matrix. Then run Lua 5.1 lint and `git diff --check`, and inspect the diff to confirm only the existing 15 fallbacks were represented.</verify>
  <done>All supported locales expose exact evidence-bound scalars, English text remains independently available, every definition has its canonical raid scope, and static tests reject byte drift or incomplete coverage.</done>
</task>

</tasks>

<verification>
- The 60/60 evidence matrix is revalidated before the first runtime locale edit.
- Locale catalogs contain exact scalar values whose byte length/digest match the evidence document.
- Only the existing 15 fallback strings are represented; no new encounter or admission behavior is added.
</verification>

<success_criteria>
- Complete, auditable exact locale data is available through the existing catalog system.
- Definition metadata is ready for exact English/current-locale matching and canonical instance scoping.
</success_criteria>

<output>
After completion, create `.planning/phases/02-locale-independent-raid-recognition/02-04-SUMMARY.md`.
</output>

---
phase: 02-locale-independent-raid-recognition
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-locale-independent-raid-recognition/02-YELL-EVIDENCE.md
autonomous: true
requirements: [LOCL-01]
must_haves:
  truths:
    - "Every one of the 15 existing yell definitions has an auditable exact string for ruRU, zhCN, esES, and frFR before any runtime locale edit."
    - "Missing, empty, conflicting, unkeyed, or unverified data stops execution instead of being guessed or silently accepted."
    - "The evidence matrix pins source revisions and proves the exact UTF-8 bytes intended for all 60 locale entries."
  artifacts:
    - path: ".planning/phases/02-locale-independent-raid-recognition/02-YELL-EVIDENCE.md"
      provides: "60/60 source-SHA, numeric-key, UTF-8 length, and digest audit matrix"
      contains: "BroadcastTextId"
  key_links:
    - from: ".planning/phases/02-locale-independent-raid-recognition/02-RESEARCH.md"
      to: ".planning/phases/02-locale-independent-raid-recognition/02-YELL-EVIDENCE.md"
      via: "the 15 fixed creature/group/text and BroadcastTextId lookup keys"
      pattern: "BroadcastTextId"
---

<objective>
Acquire and validate the exact WotLK 3.3.5a locale data required by the existing monster-yell fallback before any functional locale string is edited.

Purpose: Make data provenance a hard executable gate, preventing guessed translations and punctuation drift.
Output: One complete 60/60 review matrix outside the packaged addon runtime.
</objective>

<execution_context>
@C:/Users/Massimo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Massimo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-locale-independent-raid-recognition/02-CONTEXT.md
@.planning/phases/02-locale-independent-raid-recognition/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract all keyed locale rows from pinned client-derived sources</name>
  <files>.planning/phases/02-locale-independent-raid-recognition/02-YELL-EVIDENCE.md</files>
  <action>Pin exact commit SHAs for AzerothCore `azerothcore-wotlk` and CMaNGOS `wotlk-db`, and use a temporary directory outside the addon package for source checkout/extraction. For each of the 15 creature/group/text and BroadcastTextId keys listed in 02-RESEARCH.md, extract ruRU, zhCN, esES, and frFR from CMaNGOS `locales/BroadcastTextLocales.sql`; cross-check AzerothCore `creature_text_locale.sql` wherever that source has a row. Write one row per encounter/locale with encounter, canonical instance, creature/group/text key, BroadcastTextId, locale, source repository SHA, source path, exact UTF-8 byte length, SHA-256 digest, and cross-source status. Record the known Algalon punctuation discrepancy explicitly. Never machine-translate, use Wowhead comments, modern-Classic text, another addon's unsourced literal, or treat current RMA English punctuation as source authority. If any locale value is absent, empty, not traceable to its numeric key, or conflicts with another present source, stop this task with the rejected row documented and do not claim the matrix complete.</action>
  <verify>Check the evidence file records both pinned SHAs, all 15 expected BroadcastTextIds, the four exact locale codes, source paths, positive UTF-8 byte lengths, 64-hex SHA-256 digests, and explicit cross-source status. No addon runtime/localization file may appear in `git diff --name-only` from this plan.</verify>
  <done>All candidate values are extracted from pinned numeric-key sources with exact-byte metadata, or execution has stopped with a concrete rejected row and no runtime edit.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce the hard 60/60 acceptance gate</name>
  <files>.planning/phases/02-locale-independent-raid-recognition/02-YELL-EVIDENCE.md</files>
  <action>Programmatically parse the matrix and require exactly 15 definitions x 4 locales = 60 unique accepted rows. Reject duplicate encounter/locale pairs, unknown BroadcastTextIds, missing source SHAs/paths/keys, non-positive lengths, invalid digests, empty values, and any missing/conflict/rejected/unresolved status. Resolve a rejected row only from another versioned client-derived WotLK 3.3.5 source or an exact `CHAT_MSG_MONSTER_YELL` capture that records client locale/build and byte digest. If 60/60 cannot be proven, STOP THE PLAN and report a structured blocker; do not weaken exact matching, silently fall back to English-only locale coverage, or authorize Plan 02-04 runtime locale edits. Record the successful parser/check command and 60/60 result in the evidence document.</action>
  <verify>Re-run the matrix parser independently and assert `accepted=60`, `unique=60`, `missing=0`, `conflicts=0`, and `invalid=0`; run `git diff --check` and confirm the only planned artifact is `02-YELL-EVIDENCE.md`.</verify>
  <done>The evidence document contains a reproducible 60/60 acceptance result; otherwise the phase is visibly blocked before any functional locale string changes.</done>
</task>

</tasks>

<verification>
- The plan changes no addon runtime, locale catalog, TOC, SavedVariables, or wire-format file.
- The matrix contains 60 unique accepted rows with pinned SHA, numeric provenance, exact byte length, and digest.
- Missing/conflicting evidence is a blocking outcome, not permission to invent a locale literal.
</verification>

<success_criteria>
- Every supported non-English locale has complete exact-source evidence for all 15 existing fallbacks.
- Plan 02-04 can mechanically bind runtime strings to the accepted matrix without further translation judgment.
</success_criteria>

<output>
After completion, create `.planning/phases/02-locale-independent-raid-recognition/02-03-SUMMARY.md`.
</output>

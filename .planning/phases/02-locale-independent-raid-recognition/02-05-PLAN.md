---
phase: 02-locale-independent-raid-recognition
plan: 05
type: execute
wave: 4
depends_on: ["02-04"]
files_modified:
  - Raid Management Addon/Init.lua
  - tests/lua/harness/40_inspect_foundations.lua
  - tests/test_inspect_dataset_behavior.py
  - tests/test_localization_contract.py
autonomous: true
requirements: [LOCL-01, LOCL-02]
must_haves:
  truths:
    - "On every supported locale, either the exact English yell or the exact current-locale yell records the existing fallback encounter in its expected active raid."
    - "Changed case, whitespace, punctuation, substring text, wrong canonical instance, or no current raid never records a yell-based boss kill."
    - "Combat-log boss detection remains unchanged and primary; yell handling stays a narrow fallback."
  artifacts:
    - path: "Raid Management Addon/Init.lua"
      provides: "Exact English/current-locale and canonical-instance-scoped yell handler"
      contains: "CHAT_MSG_MONSTER_YELL"
    - path: "tests/lua/harness/40_inspect_foundations.lua"
      provides: "Exact-match, mixed-language, and wrong-instance behavior regressions"
    - path: "tests/test_inspect_dataset_behavior.py"
      provides: "Focused Lua case registration"
    - path: "tests/test_localization_contract.py"
      provides: "Static handler and definition wiring contracts"
  key_links:
    - from: "Raid Management Addon/Init.lua"
      to: "Raid Management Addon/Localization/localization.en.lua"
      via: "lookup of English-owned fallback definitions and current-locale scalar key"
      pattern: "BossYell"
    - from: "Raid Management Addon/Init.lua"
      to: "Raid Management Addon/Modules/Dataset/LootSourcesData.lua"
      via: "active canonical instance equality before Raid:AddBoss"
      pattern: "GetActiveInstanceKey"
---

<objective>
Wire the evidence-backed locale definitions into the existing monster-yell fallback with exact matching and canonical raid scoping.

Purpose: Restore fallback encounter recording on every supported locale without accepting ambiguous text in the wrong raid.
Output: A WotLK-compatible exact yell resolver in Init plus focused runtime/static regressions.
</objective>

<execution_context>
@C:/Users/Massimo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Massimo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-locale-independent-raid-recognition/02-CONTEXT.md
@.planning/phases/02-locale-independent-raid-recognition/02-RESEARCH.md
@.planning/phases/02-locale-independent-raid-recognition/02-04-SUMMARY.md
@Raid Management Addon/Init.lua
@Raid Management Addon/Modules/Dataset/LootSourcesData.lua
@Raid Management Addon/Localization/localization.en.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Specify exact mixed-language matching and canonical instance scope</name>
  <files>tests/lua/harness/40_inspect_foundations.lua, tests/test_inspect_dataset_behavior.py, tests/test_localization_contract.py</files>
  <action>Add table-driven behavior coverage for all 15 definitions and every supported non-English locale. For each definition, prove exact current-locale text matches in its expected active canonical instance and exact English also matches after a non-English locale override. Prove one-character case, whitespace, punctuation, and substring variations do not match. Prove exact text in a wrong active canonical instance and with no current raid does not call `Raid:AddBoss`. Add transition coverage ensuring recognized-to-unknown clears the active key. Keep an explicit assertion that `COMBAT_LOG_EVENT_UNFILTERED` delegates exactly as before. Add static contracts prohibiting normalization helpers/pattern matching in the yell path and requiring an expected instance on every definition.</action>
  <verify>Run `python -m unittest tests.test_inspect_dataset_behavior tests.test_localization_contract`. Static cases must execute; if Lua 5.1 is missing, record behavior cases as runner-blocked and retain them for Lua/in-game verification.</verify>
  <done>Tests cover exact English/current-locale matching, all negative text variants, wrong/no instance rejection, stale-key clearing, definition scope, and unchanged combat-log delegation.</done>
</task>

<task type="auto">
  <name>Task 2: Scope the existing yell fallback to exact text and active canonical raid</name>
  <files>Raid Management Addon/Init.lua</files>
  <action>Replace the direct `L.BossYells[text]` lookup in `CHAT_MSG_MONSTER_YELL` with the smallest Lua 5.1-compatible lookup over Plan 02-04's verified definitions. Accept only direct equality with `definition.englishText` or `L[definition.localeKey]`; do not lowercase, trim, normalize punctuation/Unicode, use substrings, patterns, or fuzzy matching. Require both `Database.GetCurrentRaid()` and equality between the authoritative active canonical instance and `definition.instanceKey` before calling `raidService:AddBoss(definition.boss)`. Keep English accepted on every locale, preserve trace behavior with matched metadata, add no fallback beyond the 15 definitions, and do not alter `COMBAT_LOG_EVENT_UNFILTERED`, SavedVariables, TOC, or wire formats.</action>
  <verify>Run the focused unittest command from Task 1, Lua 5.1 lint, `xpcall` scan, and `git diff --check`. Run `rg -n -C 8 "CHAT_MSG_MONSTER_YELL|COMBAT_LOG_EVENT_UNFILTERED" "Raid Management Addon/Init.lua"` and confirm yell matching is instance-scoped while combat-log delegation is unchanged.</verify>
  <done>The handler records only exact English/current-locale text in the definition's active canonical raid, rejects all wrong/no-instance cases, and leaves combat-log detection and compatibility contracts unchanged.</done>
</task>

</tasks>

<verification>
- Locale digest tests pass against the 60/60 evidence matrix before runtime behavior is complete.
- Exact equality is the only yell comparison and every match requires the expected active canonical instance plus a current raid.
- Full Lua 5.1/static validators pass; Lua runtime limitations are reported accurately.
</verification>

<success_criteria>
- Existing yell-dependent encounters remain recordable from exact English or current-locale text on all supported locales.
- Ambiguous/wrong-instance and altered-text inputs cannot create boss records.
- No new fallback, display-name gate, SavedVariables field, TOC entry, or wire-format change is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/02-locale-independent-raid-recognition/02-05-SUMMARY.md`.
</output>

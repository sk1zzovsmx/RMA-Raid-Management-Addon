---
phase: 02-locale-independent-raid-recognition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Raid Management Addon/Init.lua
  - Raid Management Addon/Services/Raid/Session.lua
  - Raid Management Addon/Services/Raid/Roster.lua
  - tests/lua/harness/30_raid_runtime.lua
  - tests/lua/harness/40_inspect_foundations.lua
  - tests/test_runtime_foundations_behavior.py
  - tests/test_inspect_dataset_behavior.py
autonomous: true
requirements: [RAID-01, RAID-02, RAID-03, LOCL-02]
must_haves:
  truths:
    - "A supported Vanilla, Burning Crusade, or Wrath raid is admitted by the canonical resolver result even when its display name is localized or conflicts with the map ID."
    - "Session and roster checks fail closed when Init has no active recognized instance context, without reusing a stale context."
    - "The localized display name remains stored and shown while same-instance decisions use only transient canonical identity."
  artifacts:
    - path: "Raid Management Addon/Init.lua"
      provides: "Authoritative resolver-to-Raid context wiring"
      contains: "ResolveInstanceKey"
    - path: "Raid Management Addon/Services/Raid/Session.lua"
      provides: "Transient canonical context and canonical same-instance checks"
    - path: "Raid Management Addon/Services/Raid/Roster.lua"
      provides: "Roster admission through the shared recognized context"
    - path: "tests/lua/harness/30_raid_runtime.lua"
      provides: "Runtime session and roster admission regressions"
    - path: "tests/lua/harness/40_inspect_foundations.lua"
      provides: "Cross-expansion resolver and display-name regressions"
  key_links:
    - from: "Raid Management Addon/Init.lua"
      to: "Raid Management Addon/Services/Raid/Session.lua"
      via: "recognized transient instance context committed only after both datasets activate"
      pattern: "instanceKey"
    - from: "Raid Management Addon/Services/Raid/Roster.lua"
      to: "Raid Management Addon/Services/Raid/Session.lua"
      via: "shared Raid context/check API instead of GetInstanceInfo or RaidZones"
      pattern: "module:Check"
---

<objective>
Make the existing dataset-backed resolver the sole admission authority for raid sessions and roster mutations.

Purpose: Remove the duplicated English display-name gates that reject localized and Vanilla raids while preserving the visible and persisted localized session name.
Output: Regression coverage plus one transient canonical-instance contract shared by Init, Session, and Roster.
</objective>

<execution_context>
@C:/Users/Massimo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Massimo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-locale-independent-raid-recognition/02-CONTEXT.md
@.planning/phases/02-locale-independent-raid-recognition/02-RESEARCH.md
@Raid Management Addon/Modules/Dataset/LootSourcesData.lua
@Raid Management Addon/Init.lua
@Raid Management Addon/Services/Raid/Session.lua
@Raid Management Addon/Services/Raid/Roster.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Specify canonical admission and localized display behavior</name>
  <files>tests/lua/harness/30_raid_runtime.lua, tests/lua/harness/40_inspect_foundations.lua, tests/test_runtime_foundations_behavior.py, tests/test_inspect_dataset_behavior.py</files>
  <action>Add focused regressions before runtime changes. Prove map ID 409 admits a non-English Molten Core display name with `addon.L.RaidZones = {}`, add one TBC case and map ID 631 for Wrath, and prove a recognized map ID wins over a conflicting supported display name. Cover nil/zero/unknown map-ID fallback accepting only an exact canonical dataset name and rejecting an unknown custom raid. At Session level, prove the created `raid.zone` remains the received localized name, a later different localized display name with the same canonical key does not create a replacement session, and a historical/current raid with no transient binding after reload binds to the recognized key without rewriting `zone`. Cover recognized-to-unknown transition clearing admission so Roster cannot mutate under stale identity. Add a static contract that `Services/Raid/Session.lua` and `Services/Raid/Roster.lua` contain no operational `RaidZones` lookup and that Roster does not call `GetInstanceInfo` or `ResolveInstanceKey`. Register only the new focused cases in the existing Python wrappers; do not create a new test framework.</action>
  <verify>Run `python -m unittest tests.test_runtime_foundations_behavior tests.test_inspect_dataset_behavior`. The static portions must run; Lua-backed cases may report only the already documented missing Lua 5.1 runner and must not be described as passing until executed with Lua 5.1.</verify>
  <done>Tests encode map-ID priority across all three expansion families, exact canonical fallback, unknown fail-closed behavior, localized display preservation, canonical same-instance comparison, stale-context clearing, and removal of independent Session/Roster display gates.</done>
</task>

<task type="auto">
  <name>Task 2: Carry one transient recognized context into Session and Roster</name>
  <files>Raid Management Addon/Init.lua, Raid Management Addon/Services/Raid/Session.lua, Raid Management Addon/Services/Raid/Roster.lua</files>
  <action>Keep `LootSourcesData.ResolveInstanceKey(instanceName, instanceMapId)` in `Init.lua` as the only recognition operation. After both LootSourcesData and IgnoredMobs activate successfully, commit the smallest Raid-owned transient context containing localized display name, canonical instance key, and resolved difficulty; clear it together with dataset deactivation on non-raid, unsupported, or failed recognition. Do not persist the key, add a SavedVariables field, change the version-5 wire format, or rewrite historical `raid.zone` values. Refactor Session checks to consume this context, retain existing size/difficulty rules, store the localized display name only when a session is created, and compare canonical key for same-instance identity. On reload/recovery, bind an existing current raid to the first recognized runtime key without changing its saved name. Refactor Roster to ask the shared Raid owner to consume/check that context; remove direct `GetInstanceInfo`, `ResolveInstanceKey`, and `L.RaidZones` admission. Keep unknown/custom raids fail closed and preserve existing no-current-raid roster cleanup.</action>
  <verify>Run the focused unittest command from Task 1, then `py -3 .agents/skills/wow-addon-dev-wotlk-v335a/scripts/lint_lua51.py "Raid Management Addon"`, `py -3 .agents/skills/wow-addon-dev-wotlk-v335a/scripts/scan_xpcall.py "Raid Management Addon"`, and `git diff --check`. Also run `rg -n "RaidZones|GetInstanceInfo|ResolveInstanceKey" "Raid Management Addon/Services/Raid/Session.lua" "Raid Management Addon/Services/Raid/Roster.lua"`; no admission dependency may remain.</verify>
  <done>Init is the only instance recognizer, Session and Roster consume one cleared/committed transient canonical context, localized stored names stay unchanged, and all changed Lua passes the WotLK/Lua 5.1 static gates.</done>
</task>

</tasks>

<verification>
- `ResolveInstanceKey` remains the only operational admission resolver and retains map-ID priority.
- Session and Roster have no `RaidZones` admission lookup or independent resolver.
- No SavedVariables schema, TOC load order, addon-message prefix, or wire payload changes.
- Lua-backed assertions are explicitly recorded as environment-blocked if no Lua 5.1 executable exists; static checks still run.
</verification>

<success_criteria>
- Vanilla 409, one TBC map, and Wrath 631 all reach the same canonical admission path.
- Localized/conflicting display text cannot veto a recognized map ID and remains presentation-only.
- Unknown/custom raids and stale contexts cannot create or update sessions or rosters.
- Historical raid names are never migrated or rewritten.
</success_criteria>

<output>
After completion, create `.planning/phases/02-locale-independent-raid-recognition/02-01-SUMMARY.md`.
</output>

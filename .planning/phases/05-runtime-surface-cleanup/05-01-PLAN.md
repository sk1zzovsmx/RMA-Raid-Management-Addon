---
phase: 05-runtime-surface-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Raid Management Addon/Modules/UI/ScreenNotice.lua
  - Raid Management Addon/Services/Master/Trade.lua
  - Raid Management Addon/Services/Loot/State.lua
  - tests/lua/harness/20_raid_database.lua
  - tests/lua/harness/30_raid_runtime.lua
  - tests/lua/harness/60_loot_ui.lua
  - tests/test_runtime_bootstrap_contract.py
  - tests/test_runtime_foundations_behavior.py
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03]
must_haves:
  truths:
    - "Screen notices still render, size, show, and fade with the requested bounded duration when published through Internal.ScreenNotice, while UI.ScreenNotice.Show no longer exists."
    - "Manual trade candidate, acceptance, uncertainty, retry, and completion behavior remains available through the existing Trade operations, while Trade.EnsureState no longer exposes mutable state."
    - "Database.EnsureLootRuntimeState directly invokes ContextState.SyncRuntimeState and returns the same normalized loot context and synchronized projections without a Loot:SyncRuntimeState forwarding method."
    - "The cleanup changes no XML, localization, SavedVariables, wire protocol, vendored library, visible text, or supported external entrypoint."
  artifacts:
    - path: "Raid Management Addon/Modules/UI/ScreenNotice.lua"
      provides: "Internal-event-owned screen notice rendering without a direct Show export"
      contains: "RegisterCallback(ScreenNoticeEvent, showNotice)"
    - path: "Raid Management Addon/Services/Master/Trade.lua"
      provides: "Private ensureState helper used only by Trade-owned public operations"
      contains: "local function ensureState()"
    - path: "Raid Management Addon/Services/Loot/State.lua"
      provides: "Direct loot-context normalization during Database.EnsureLootRuntimeState"
      contains: "ContextState.SyncRuntimeState(raidState)"
    - path: "tests/lua/harness/30_raid_runtime.lua"
      provides: "Focused internal-event screen notice regression"
      contains: "screen_notice_uses_internal_event_without_direct_export"
    - path: "tests/lua/harness/60_loot_ui.lua"
      provides: "Manual trade behavior regression that does not consume the removed state accessor"
      contains: "loot_manual_hold_trade_requires_inventory_evidence"
    - path: "tests/lua/harness/20_raid_database.lua"
      provides: "Focused direct loot runtime-state normalization regression"
      contains: "loot_runtime_state_syncs_directly_without_service_forwarder"
  key_links:
    - from: "Raid Management Addon/Services/Raid/LootMethod.lua"
      to: "Raid Management Addon/Modules/UI/ScreenNotice.lua"
      via: "TriggerEvent(Internal.ScreenNotice, message, duration) reaches the registered local showNotice callback"
      pattern: 'RegisterCallback\(ScreenNoticeEvent, showNotice\)'
    - from: "Raid Management Addon/Services/Master/Trade.lua"
      to: "Raid Management Addon/Services/Loot/State.lua"
      via: "Trade-owned operations call local ensureState, which obtains canonical runtime state through Database.EnsureLootRuntimeState"
      pattern: 'local state = ensureState\(\)'
    - from: "Raid Management Addon/Services/Loot/State.lua"
      to: "Raid Management Addon/Services/Loot/State.lua"
      via: "Database.EnsureLootRuntimeState calls the same-file ContextState owner directly before continuing loot-state normalization"
      pattern: 'lootContext = ContextState.SyncRuntimeState\(raidState\)'
---

<objective>
Remove the three confirmed unconsumed runtime exports or forwarders while preserving their supported behavior and ownership boundaries.

Purpose: Reduce the addon's runtime surface to the event and service owners that actually carry behavior, without introducing compatibility scaffolding or changing product contracts.
Output: Three small runtime deletions/direct wiring changes, focused regressions for each path, and one final compatibility verification pass.
</objective>

<execution_context>
@C:/Users/Massimo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Massimo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-runtime-surface-cleanup/05-CONTEXT.md
@AGENTS.md
@docs/API_SURFACE.md
@Raid Management Addon/Modules/UI/ScreenNotice.lua
@Raid Management Addon/Services/Raid/LootMethod.lua
@Raid Management Addon/Services/Master/Trade.lua
@Raid Management Addon/Services/Loot/Context.lua
@Raid Management Addon/Services/Loot/State.lua
@tests/lua/harness/20_raid_database.lua
@tests/lua/harness/30_raid_runtime.lua
@tests/lua/harness/60_loot_ui.lua
@tests/test_loot_distribution_hardening_behavior.py
@tests/test_runtime_bootstrap_contract.py
@tests/test_runtime_foundations_behavior.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keep screen notices on the internal event path only</name>
  <files>Raid Management Addon/Modules/UI/ScreenNotice.lua, tests/lua/harness/30_raid_runtime.lua, tests/test_runtime_foundations_behavior.py</files>
  <action>Add the focused Lua case `screen_notice_uses_internal_event_without_direct_export` and register one matching Python test in `RuntimeFoundationsBehaviorTest`. Build only the minimal fake `RMAScreenNoticeFrame`, title/detail font strings, `UI.Effects.SetTimedFade`, and Bus callback capture needed to load the production module. Assert that the module registers `showNotice` for `addon.Events.Internal.ScreenNotice`, `addon.UI.ScreenNotice.Show` is nil, and invoking the captured callback with the event name, a non-empty message, and a requested duration preserves the existing title colorization, detail hiding, frame sizing/show/alpha updates, and fade arguments. Also retain the empty-message false result so the callback contract is protected without creating a new API.

Then delete only `ScreenNotice.Show` from `Modules/UI/ScreenNotice.lua`. Keep the local `showNotice` signature and body, constants, frame lookup, duration clamping, fade callback, and `RegisterCallback(ScreenNoticeEvent, showNotice)` unchanged. Do not add an alias, deprecated shim, replacement method, XML change, localization change, or direct controller call. Commit the completed source-and-regression task atomically as `refactor(05-01): internalize screen notice invocation`.</action>
  <verify>Run `py -3 -m unittest tests.test_runtime_foundations_behavior.RuntimeFoundationsBehaviorTest.test_screen_notice_uses_internal_event_without_direct_export`. Then run `rg -n "function ScreenNotice\.Show" "Raid Management Addon" -g "*.lua"`; the search must return no matches, while `rg -n "RegisterCallback\(ScreenNoticeEvent, showNotice\)" "Raid Management Addon/Modules/UI/ScreenNotice.lua"` must return the event registration.</verify>
  <done>The internal ScreenNotice event still drives the same visible callback behavior and duration/fade contract, and no callable `UI.ScreenNotice.Show` export remains.</done>
</task>

<task type="auto">
  <name>Task 2: Keep mutable Trade state behind owning operations</name>
  <files>Raid Management Addon/Services/Master/Trade.lua, tests/lua/harness/60_loot_ui.lua</files>
  <action>Update the existing `loot_manual_hold_trade_requires_inventory_evidence` regression so it no longer consumes `Trade.EnsureState`. Immediately after loading the production Trade module, assert the removed export is nil. Replace the two internal-state reads with existing behavioral/public-operation evidence: after failed verification, `CompletePending` remains false, the warning count is exactly 1, logger and counter counts are exactly 0, and `HasClosePending()` is true; after the inventory delta and successful retry, the warning count remains exactly 1, logger and counter counts are exactly 1, and `HasClosePending()` is false. Keep the existing multi-candidate partial logger retry assertions intact.

Delete only the `Trade.EnsureState` public method from `Services/Master/Trade.lua`. Preserve the local `ensureState` helper and every existing call to it from Trade-owned operations, along with all remaining public signatures, return values, candidate matching, selected reasons, pending-confirmation behavior, uncertainty handling, logging, and counter updates. Do not expose a replacement accessor or move state to another module. Commit atomically as `refactor(05-01): privatize trade state initialization`.</action>
  <verify>Run `py -3 -m unittest tests.test_loot_distribution_hardening_behavior.LootDistributionHardeningTests.test_manual_hold_trade_requires_inventory_evidence` and confirm the case proves both transitions exactly: failed verification yields warnings=1, logger=0, counter=0, and `HasClosePending()==true`; successful retry leaves warnings=1, advances logger=1 and counter=1, and yields `HasClosePending()==false`. Then run `rg -n "function Trade\.EnsureState" "Raid Management Addon" -g "*.lua"`; it must return no matches, and `rg -n "local function ensureState|local state = ensureState\(\)" "Raid Management Addon/Services/Master/Trade.lua"` must confirm private ownership remains in use.</verify>
  <done>Existing manual trade behavior and return contracts remain covered through Trade operations, while mutable state can no longer be obtained through `Trade.EnsureState`.</done>
</task>

<task type="auto">
  <name>Task 3: Normalize loot runtime state through the same-file owner directly</name>
  <files>Raid Management Addon/Services/Loot/State.lua, tests/lua/harness/20_raid_database.lua, tests/test_runtime_bootstrap_contract.py</files>
  <action>Add the focused Lua case `loot_runtime_state_syncs_directly_without_service_forwarder` and register one matching Python test in `RuntimeBootstrapContractTest`. Load the production Loot context and state owners with a raid-state fixture containing legacy event-boss, active-window/source, sessions, and snapshots values that exercise the current normalizers. Call `Database.EnsureLootRuntimeState` and assert the returned raid state and loot context retain identity, canonical numeric/default normalization, synchronized `activeLoot` plus legacy `activeWindow`/`source` projections, normalized session/snapshot maps, and the established loot-state defaults. Assert `addon.Services.Loot.SyncRuntimeState` is nil while the owning `addon.Services.Loot._State.SyncRuntimeState` remains present; do not invent a replacement public entrypoint.

In `Database.EnsureLootRuntimeState`, replace the guarded `Loot:SyncRuntimeState(raidState)` forwarding call with the direct same-file `ContextState.SyncRuntimeState(raidState)` call at the same position, retaining assignment of the returned context to `raidState.lootContext`. Delete only `module:SyncRuntimeState`. Keep all normalizers, `_State` methods, bootstrap ordering, state defaults, legacy projections, and return tuple unchanged. Do not touch persistence globals or schemas. Commit atomically as `refactor(05-01): remove loot state forwarder`.

After the focused test passes, run the full relevant automated suite exactly once, followed by the WotLK 3.3.5a static validators and scope guards listed below. Do not edit unrelated runtime or tests to chase hypothetical coverage; if a gate fails, diagnose the evidenced cause and rerun the nearest failed gate before repeating any broader command.</action>
  <verify>First run `py -3 -m unittest tests.test_runtime_bootstrap_contract.RuntimeBootstrapContractTest.test_loot_runtime_state_syncs_directly_without_service_forwarder`. Confirm `rg -n "Loot:SyncRuntimeState|function module:SyncRuntimeState" "Raid Management Addon/Services/Loot/State.lua"` returns no matches and `rg -n "lootContext = ContextState\.SyncRuntimeState\(raidState\)" "Raid Management Addon/Services/Loot/State.lua"` returns exactly the direct bootstrap call. Then run once: `py -3 -m unittest discover -s tests`; `py -3 .agents/skills/wow-addon-dev-wotlk-v335a/scripts/validate_toc.py "Raid Management Addon/Raid Management Addon.toc"`; `py -3 .agents/skills/wow-addon-dev-wotlk-v335a/scripts/lint_lua51.py "Raid Management Addon"`; `py -3 .agents/skills/wow-addon-dev-wotlk-v335a/scripts/scan_xpcall.py "Raid Management Addon"`; `rg -n "&lt;Scripts&gt;|&lt;On[A-Za-z]+&gt;" "Raid Management Addon/UI" -g "*.xml"`; and `git diff --check`. The XML search must return no matches. Inspect `git diff --name-only` and confirm no XML, Localization, `Libs/`, TOC, SavedVariables owner, communication owner, Logger, or Attendance file changed.</verify>
  <done>Loot bootstrap directly normalizes the same context at the same ordering point without a Loot service forwarder, all focused regressions pass, the complete relevant suite and WotLK static gates are clean, and the diff stays inside the approved cleanup surface.</done>
</task>

</tasks>

<verification>
- Each focused regression passes before moving to the next removal, with the complete relevant unit-test discovery run once only after all three changes are present.
- The three removed symbols have no runtime definitions or calls; the internal ScreenNotice registration, local Trade state helper, and `_State.SyncRuntimeState` owner remain connected.
- TOC validation, Lua 5.1 lint, variadic-`xpcall` scan, XML layout-only scan, and `git diff --check` are clean.
- The final diff contains no UI primitive extraction, Logger/Attendance work, XML, localization, SavedVariables, wire/protocol, vendored library, TOC, or new API change.
</verification>

<success_criteria>
- Screen notices preserve their current event-driven frame and fade behavior without `ScreenNotice.Show`.
- Trade workflows preserve their current behavioral and return contracts without `Trade.EnsureState` or any replacement accessor.
- Loot runtime bootstrap produces the same normalized context through a direct `ContextState.SyncRuntimeState` call without `Loot:SyncRuntimeState`.
- CLEAN-01, CLEAN-02, and CLEAN-03 are covered once in this bounded plan and all applicable automated compatibility gates pass.
</success_criteria>

<output>
After completion, create `.planning/phases/05-runtime-surface-cleanup/05-01-SUMMARY.md`.
</output>

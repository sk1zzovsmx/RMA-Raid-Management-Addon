---
phase: 04-milestone-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Raid Management Addon/Widgets/LootCounter.lua
  - tests/lua/harness/20_raid_database.lua
  - tests/lua/harness/30_raid_runtime.lua
  - tests/lua/harness/40_inspect_foundations.lua
  - tests/lua/harness/70_raid_sync.lua
  - tests/test_raid_replication_behavior.py
autonomous: true
requirements: [QUAL-01, QUAL-02]
must_haves:
  truths:
    - "Proprietary runtime Lua is ASCII-only outside intentional Localization data and vendored Libs."
    - "All 507 currently discoverable tests execute through LuaJIT and pass without production fallbacks, exclusions, or weakened assertions."
    - "The TOC, Lua 5.1, variadic-xpcall, XML-handler, forbidden-modern-API, feature-OnUpdate, stale-branding, and diff gates are clean under their documented zero-match or exact-inventory contracts."
  artifacts:
    - path: "Raid Management Addon/Widgets/LootCounter.lua"
      provides: "ASCII-only loot counter runtime source"
      contains: "FREE -> OS -> MS"
    - path: "tests/lua/harness/20_raid_database.lua"
      provides: "Valid canonical archive reload-position fixture aligned with quarantine preservation"
      contains: "raid_replication_archive_reload"
    - path: "tests/lua/harness/30_raid_runtime.lua"
      provides: "Complete quarantine, canonical-session, and unknown-raid fixtures"
    - path: "tests/lua/harness/40_inspect_foundations.lua"
      provides: "Complete canonical dataset identity fixture diagnostics"
      contains: "instance_datasets_share_canonical_identity"
    - path: "tests/lua/harness/70_raid_sync.lua"
      provides: "Live-sync fixture with committed canonical raid context"
      contains: "raid_live_sync_group_loot_leader_authority"
    - path: "tests/test_raid_replication_behavior.py"
      provides: "Reload regression method named for the valid archive contract"
  key_links:
    - from: "tests/test_raid_replication_behavior.py"
      to: "tests/lua/harness/20_raid_database.lua"
      via: "Python method executes the existing raid_replication_archive_reload Lua case"
      pattern: "raid_replication_archive_reload"
    - from: "tests/lua/harness/30_raid_runtime.lua"
      to: "Raid Management Addon/Init.lua"
      via: "Fixtures provide every localization and diagnostic key exercised by bootstrap and bounded retry paths"
      pattern: "LogRaidUnknownInstance"
    - from: "tests/lua/harness/70_raid_sync.lua"
      to: "Raid Management Addon/Init.lua"
      via: "Both simulated clients commit the recognized ICC canonical context before scheduling instance checks"
      pattern: "ScheduleInstanceChecks"
---

<objective>
Close every demonstrated automated blocker and run one reproducible final repository gate.

Purpose: Establish QUAL-01 and QUAL-02 with evidence from the complete LuaJIT-backed suite and the existing WotLK-specific validators, without changing runtime behavior or expanding tooling.
Output: One ASCII-only comment correction, six minimally repaired test contracts, and a clean final automated gate recorded in the plan summary.
</objective>

<execution_context>
@C:/Users/Massimo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Massimo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-milestone-verification/04-CONTEXT.md
@.planning/phases/04-milestone-verification/04-RESEARCH.md
@AGENTS.md
@Raid Management Addon/Widgets/LootCounter.lua
@tests/lua_test_runner.py
@tests/lua/harness/20_raid_database.lua
@tests/lua/harness/30_raid_runtime.lua
@tests/lua/harness/40_inspect_foundations.lua
@tests/lua/harness/70_raid_sync.lua
@tests/test_inspect_dataset_behavior.py
@tests/test_runtime_foundations_behavior.py
@tests/test_raid_replication_behavior.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the one demonstrated proprietary non-ASCII character</name>
  <files>Raid Management Addon/Widgets/LootCounter.lua</files>
  <action>At `Widgets/LootCounter.lua:418`, replace only the `U+2192` arrow in the comment with the ASCII sequence `-&gt;`, yielding `FREE -&gt; OS -&gt; MS`. Do not alter logic, other comments, intentional non-English files under `Localization/`, or vendored `Libs/`. Immediately scan every proprietary runtime `.lua` file below `Raid Management Addon`, excluding top-level `Localization` and `Libs`, by reading UTF-8 with Python and failing if any code point is greater than 127. If another hit appears, stop and report the changed evidence instead of broadening the edit. Commit this one-file correction atomically as `chore(04-01): make loot counter comment ASCII`.</action>
  <verify>Run a Python `pathlib.Path('Raid Management Addon').rglob('*.lua')` scan that skips paths whose relative parts contain `Localization` or `Libs`, prints `path:line:U+XXXX` for every code point above 127, and exits nonzero when hits exist; it must print zero hits. Then run `git diff --check`.</verify>
  <done>The known arrow is ASCII, no other proprietary runtime Lua code point above 127 exists outside the two explicit exclusions, and no unrelated file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Repair the six demonstrated stale harness contracts</name>
  <files>tests/lua/harness/20_raid_database.lua, tests/lua/harness/30_raid_runtime.lua, tests/lua/harness/40_inspect_foundations.lua, tests/lua/harness/70_raid_sync.lua, tests/test_raid_replication_behavior.py</files>
  <action>Make only the fixture corrections demonstrated by the 507-test baseline. In `40_inspect_foundations.lua`, seed the localization warning and diagnostic templates used by `instance_datasets_share_canonical_identity`. In `30_raid_runtime.lua`, add `LogRaidUnknownInstance` to the unknown-retry diagnostic fixture; initialize the case-local `fixtureAddon.L` table before assigning quarantine labels; and give the historical canonical-session record the valid size and difficulty matching the first recognized context. In `20_raid_database.lua`, change `raid_replication_archive_reload` to begin from nil or an explicit valid format-1 canonical archive so it tests reload position without expecting an unsupported beta array to be destroyed. In `test_raid_replication_behavior.py`, rename `test_beta_store_resets_and_reload_preserves_sync_position` to `test_valid_archive_reload_preserves_sync_position` while continuing to execute the same Lua case. In `70_raid_sync.lua`, explicitly commit the recognized ICC canonical context for both simulated clients before `ScheduleInstanceChecks()`. Preserve all warning-dedupe, canonical-identity-change, authority, R5 snapshot, convergence, unsupported/future archive preservation, and quarantine assertions. Do not add nil guards or fallbacks to production code, restore destructive migration, change wire/SavedVariables contracts, or weaken assertions. Commit atomically as `test(04-01): align cross-phase harness contracts`.</action>
  <verify>Create the external LuaJIT alias exactly as documented in `04-RESEARCH.md`: an idempotent hard link `%TEMP%\rma-phase4-lua-alias\lua.exe` targeting `C:\tools\LuaJIT\bin\luajit.exe`, then prepend both the alias directory and `C:\tools\LuaJIT\bin` to this process's `PATH`. Run these six Python methods together: `tests.test_inspect_dataset_behavior.InspectDatasetBehaviorTests.test_instance_datasets_share_canonical_identity`, `tests.test_runtime_foundations_behavior.RuntimeFoundationsBehaviorTest.test_unknown_raid_retry_recovers_without_warning_spam`, `tests.test_runtime_foundations_behavior.RuntimeFoundationsBehaviorTest.test_bootstrap_raid_archive_quarantine_is_degraded_and_recovers`, `tests.test_raid_replication_behavior.RaidReplicationBehaviorTests.test_valid_archive_reload_preserves_sync_position`, `tests.test_runtime_foundations_behavior.RuntimeFoundationsBehaviorTest.test_raid_session_uses_transient_canonical_identity`, and `tests.test_raid_replication_behavior.RaidReplicationBehaviorTests.test_group_loot_uses_raid_leader_as_authority`. All six must pass. For any one cause, allow no more than two repair cycles without new evidence; then stop and report rather than adding code.</verify>
  <done>All six previously failing cases pass through the existing LuaJIT-backed runner, and their assertions express current Phase 1-3 contracts without any production runtime change.</done>
</task>

<task type="auto">
  <name>Task 3: Close focused policy gates before the one final full suite</name>
  <files>None (read-only verification; the output step creates 04-01-SUMMARY.md)</files>
  <action>Keep the external LuaJIT alias first on `PATH`, but do not run complete discovery yet. First run the repository-owned TOC, Lua 5.1, and variadic-`xpcall` validators; scan proprietary XML for script handlers; scan proprietary Lua/XML for `C_Timer`, `C_AddOns`, `Settings.*`, `MenuUtil`, `SetAtlas`, and `SetColorTexture`; inventory proprietary Lua `OnUpdate` bindings and require exactly the established shared scheduler at `Modules/Timer.lua:164` with no feature-frame polling; repeat the Task 1 ASCII scan; run the external-input branding scan described below; and run `git diff --check`.

For retired branding, require the named environment variable `RMA_RETIRED_IDENTIFIER_PATTERN_FILE`. It must resolve to an existing regular file outside the repository containing exactly one non-empty regex line. Resolve both the repository root and input path, block if the input is inside the repository, read the line without printing it, and pass it as the pattern to `rg` across proprietary `.lua`, `.xml`, `.toc`, and `.md` files with `Libs/` excluded. If the variable is absent, the path is missing/in-repository, or the content is empty/multiline, mark the gate blocked; never substitute a guessed pattern or write its value into Git artifacts. Treat `rg` exit 1 as the required zero-match result, exit 0 as a blocking stale-identifier finding, and exit 2 or greater as tool failure.

For a focused static/policy failure, correct only the evidenced cause within phase scope, rerun that nearest gate, and permit at most two repair cycles for the same cause without new evidence. Do not create validators, exclusions, fallbacks, abstractions, wire changes, migrations, or general refactors. Only after every focused gate is clean on the final tree, run the LuaJIT-backed `python -m unittest discover` exactly once as the last automated command and require 507/507. If this final full run fails, stop the plan and report the failing evidence without editing or blindly repeating it; a later evidence-driven correction must culminate in a new replacement final full-suite run against its own final tree. Do not modify a planning artifact in this task: retain concise command/status/count notes for the standard `<output>` step, which alone creates `04-01-SUMMARY.md`.</action>
  <verify>Using the bundled Python from `C:\Users\Massimo\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe`, first run `validate_toc.py 'Raid Management Addon/Raid Management Addon.toc'`; `lint_lua51.py 'Raid Management Addon' 'tests/lua'`; and `scan_xpcall.py 'Raid Management Addon'`. Then run `rg -n '&lt;Scripts&gt;|&lt;On[A-Za-z]+&gt;' 'Raid Management Addon' -g '*.xml' -g '!**/Libs/**'`, the forbidden-modern-API scan, `rg -n 'SetScript\s*\(\s*["'']OnUpdate["'']' 'Raid Management Addon' -g '*.lua' -g '!**/Libs/**'`, the Task 1 ASCII scan, and `git diff --check`. In PowerShell, require `$env:RMA_RETIRED_IDENTIFIER_PATTERN_FILE`; use `Resolve-Path`, `Get-Item`, and the resolved repository root to reject a missing path, directory, or in-repository file; require `@(Get-Content -LiteralPath $resolvedPatternFile | Where-Object { -not [string]::IsNullOrWhiteSpace($_) }).Count -eq 1`; assign the trimmed line to `$taskRetiredPattern` without emitting it; then run `rg -n --hidden -g '*.lua' -g '*.xml' -g '*.toc' -g '*.md' -g '!**/Libs/**' -g '!.git/**' -- $taskRetiredPattern .`. Require validator exit 0, zero prohibited XML/API/branding/ASCII matches, and exactly the single shared timer inventory match. Only then, as the final command, run `&amp; $pythonExe -m unittest discover -s tests -p 'test_*.py'` and require 507/507; do not rerun it after a failure inside this plan.</verify>
  <done>QUAL-01 and QUAL-02 have reproducible, sanitized automated evidence; no unavailable tool is called a pass and no live-client behavior is inferred from automation.</done>
</task>

</tasks>

<verification>
- The only runtime edit is the ASCII spelling of one comment; production logic, TOC, SavedVariables, protocols, and vendored libraries are untouched.
- The six focused methods pass before complete discovery runs, and complete discovery reports 507/507 through LuaJIT.
- All WotLK/Lua/static policy gates meet their zero-match or exact-one-inventory contracts.
- The summary distinguishes automated evidence from the still-pending real-client evidence in Plan 04-02.
</verification>

<success_criteria>
- QUAL-01 is satisfied by a zero-hit proprietary runtime ASCII scan with intentional locale data and Libs excluded.
- QUAL-02 is satisfied by 507/507 tests plus clean TOC, Lua 5.1, xpcall, XML, API, branding, timer-inventory, and diff gates.
- No automated result claims SavedVariables writeback, real localized traffic, combat safety, or taint cleanliness.
</success_criteria>

<output>
After completion, create `.planning/phases/04-milestone-verification/04-01-SUMMARY.md`.
</output>

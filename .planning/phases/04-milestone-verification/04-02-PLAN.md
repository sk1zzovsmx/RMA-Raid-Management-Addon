---
phase: 04-milestone-verification
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .planning/phases/04-milestone-verification/04-IN-GAME-CHECKLIST.md
autonomous: false
requirements: [QUAL-03]
must_haves:
  truths:
    - "A human-observed clean, valid, future-format, and restored-valid client cycle proves the six canonical RMA SavedVariables initialize, persist, quarantine without data loss, and recover without a persisted flag."
    - "An English authority and one supported non-English member recognize the same canonical raid and visibly converge through raid-history, Reserves, and Distribution version-5 synchronization."
    - "Real Reserves and Distribution requests succeed first, produce no duplicate response work on an immediate repeat, and succeed again after at least five seconds."
    - "One exact real server-emitted catalog yell records only inside its expected canonical raid, while altered or out-of-scope text does not record."
    - "Normal RMA entrypoints and UI interactions during combat produce no RMA Lua error, blocked/forbidden action, protected-action failure, or attributable clean-session taint entry."
  artifacts:
    - path: ".planning/phases/04-milestone-verification/04-IN-GAME-CHECKLIST.md"
      provides: "Versioned, privacy-safe, human-observed milestone acceptance record"
      contains: "Status"
  key_links:
    - from: ".planning/phases/04-milestone-verification/04-IN-GAME-CHECKLIST.md"
      to: "Raid Management Addon/Raid Management Addon.toc"
      via: "Checklist records the exact tested commit, addon version, client build, and Interface 30300 environment"
      pattern: "30300"
    - from: ".planning/phases/04-milestone-verification/04-IN-GAME-CHECKLIST.md"
      to: "Raid Management Addon/Database/SavedVariables.lua"
      via: "Sanitized before/after archive-block digest and observed quarantine/recovery behavior"
      pattern: "RMA_Raids"
    - from: ".planning/phases/04-milestone-verification/04-IN-GAME-CHECKLIST.md"
      to: "Raid Management Addon/Services/Loot/DistributionSession.lua"
      via: "Two-client real request timing and visible convergence observations"
      pattern: "RequestSnapshot"
---

<objective>
Obtain the real-client evidence that automation cannot supply and keep it blocking until every required observation is complete.

Purpose: Satisfy QUAL-03 with repeatable, privacy-safe observations of SavedVariables writeback, localized server behavior, multi-client synchronization, combat lockdown, and taint rather than synthetic or inferred results.
Output: A completed `04-IN-GAME-CHECKLIST.md` whose every required row is human-observed `PASS`, or an explicit blocking `PENDING`/`FAIL` that keeps the phase open.
</objective>

<execution_context>
@C:/Users/Massimo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Massimo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-milestone-verification/04-CONTEXT.md
@.planning/phases/04-milestone-verification/04-RESEARCH.md
@.planning/phases/04-milestone-verification/04-01-SUMMARY.md
@AGENTS.md
@Raid Management Addon/Raid Management Addon.toc
@Raid Management Addon/Init.lua
@Raid Management Addon/Modules/UI/Frames.lua
@Raid Management Addon/Services/Reserves/Sync.lua
@Raid Management Addon/Services/Loot/DistributionSession.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare the privacy-safe in-game acceptance checklist</name>
  <files>.planning/phases/04-milestone-verification/04-IN-GAME-CHECKLIST.md</files>
  <action>Create the checklist with every acceptance row initially `PENDING`. Include metadata fields for exact Git commit, TOC version, WotLK build/Interface 30300, server/test environment, date, English-authority and supported non-English-member locales/roles, selected catalogued encounter, addon profile, SavedVariables profile category, and clean taint-log session. Use numbered rows or compact tables with `Expected`, `Actual`, `Status`, and privacy-safe `Evidence` for: preflight; clean profile; valid six-variable reload; unsupported future archive quarantine; backup recovery; canonical localized raid identity; real scoped yell and negative controls; raid-history sync; Reserves first/immediate-repeat/post-five-second requests; Distribution first/immediate-repeat/post-five-second requests; combat UI; Lua/protected errors; and fresh taint-log inspection. State that only `PASS`, `FAIL`, and `PENDING` are valid, every required row must be `PASS` for QUAL-03, and an unavailable client/locale/encounter remains `PENDING`. Add explicit privacy rules: never commit WTF files, SavedVariables contents, full logs, account/character names, loot records, or private identifiers; allow only sanitized markers/digests, concise excerpts, and external log filenames. Do not add the checklist to the TOC or package, generate fixtures containing user data, or prefill any observed result from automation. Commit the PENDING template atomically as `docs(04-02): prepare in-game acceptance checklist`.</action>
  <verify>Run `rg -n 'PENDING|Expected|Actual|Status|Evidence|30300|RMA_Raids|RMA_Options|five seconds|taint|ADDON_ACTION_BLOCKED|ADDON_ACTION_FORBIDDEN' '.planning/phases/04-milestone-verification/04-IN-GAME-CHECKLIST.md'` and confirm all required groups are present and none is marked PASS. Run `git diff --check` and confirm the TOC is unchanged.</verify>
  <done>A versioned checklist exists with exact environment/evidence fields, all live requirements are PENDING, and no private client artifact or synthetic result is stored.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Observe SavedVariables reload, quarantine, and recovery in game</name>
  <files>.planning/phases/04-milestone-verification/04-IN-GAME-CHECKLIST.md</files>
  <action>Pause for the human to execute the single-client steps below. On resume, require row-by-row Actual, Status, and sanitized Evidence rather than accepting a bare approval. Copy only the supplied observations into the checklist. Do not inspect or commit WTF data and do not convert missing observations into PASS. If any row is FAIL, stop for an evidence-driven gap plan; if any row is missing, leave it PENDING and keep this checkpoint blocked.</action>
  <what-built>A PENDING checklist for clean, valid, future-format, and recovered-valid SavedVariables profiles plus normal single-client UI behavior.</what-built>
  <how-to-verify>
1. Close the WotLK client. Work only on disposable SavedVariables copies and retain a valid backup. Do not edit the only copy of real account data.
2. For the clean profile, remove only the disposable RMA SavedVariables state, log in with the tested build, and verify no RMA Lua error; only `RMA_Raids`, `RMA_Players`, `RMA_Reserves`, `RMA_Warnings`, `RMA_Spammer`, and `RMA_Options` initialize; `/rma`, minimap/QuickBar, and principal windows open normally.
3. With the client closed, install a valid disposable profile containing representative values for all six canonical keys and a recognizable sanitized history marker. Log in, inspect it, `/reload`, then logout/login; verify the marker and representative values remain readable and unrelated features remain usable.
4. With the client closed, install the representative unsupported future-format `RMA_Raids` profile. Capture a privacy-safe digest of only that isolated serialized archive block. Log in/reload and verify exactly one quarantine warning for the cycle, the block digest remains equivalent, history is visibly read-only/unavailable, history mutation and replication are rejected, and configuration, Reserves, warnings, spammer, and unrelated features still operate.
5. Close the client, restore the valid backup, then log in/reload. Verify normal history automatically returns with no persisted quarantine flag or confirmation and without deleting all six keys.
6. For every checklist row, provide observed `Actual`, `PASS`/`FAIL`, and a concise sanitized evidence note. Do not attach or commit WTF files, full logs, player names, loot contents, or account identifiers.
  </how-to-verify>
  <resume-signal>Reply with the numbered observed results and sanitized evidence for every single-client/SavedVariables row. A bare approval is insufficient. The executor must copy only those human observations into the checklist; any FAIL or missing row remains blocking and must not be self-certified.</resume-signal>
  <verify>Read the updated checklist and confirm every preflight, clean-profile, valid-profile, future-format quarantine, and recovery row has human-supplied Actual and Evidence plus Status PASS. Confirm no private data or full client artifact entered Git and run `git diff --check`.</verify>
  <done>Every single-client and SavedVariables row is backed by explicit human observation and marked PASS; otherwise the task remains blocked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Observe localized synchronization, combat, and taint in game</name>
  <files>.planning/phases/04-milestone-verification/04-IN-GAME-CHECKLIST.md</files>
  <action>Pause for the human to execute the two-client and clean combat/taint steps below. On resume, require row-by-row Actual, Status, and sanitized Evidence rather than accepting a bare approval. Copy only the supplied observations into the checklist and verify every required row is PASS before completion. Do not infer live behavior from Plan 04-01, synthetic helpers, or a visually working UI. If a row is FAIL, stop for a minimal evidence-driven gap plan; if the locale, second client, encounter, log environment, or an observation is unavailable, leave it PENDING and keep the phase open.</action>
  <what-built>The same checklist, now awaiting real two-client localized synchronization, scoped server yell, combat-lockdown, Lua-error, and clean-taint observations.</what-built>
  <how-to-verify>
1. Close both clients; archive or remove the previous taint log, use an RMA-only addon profile where practical, enable `/console scriptErrors 1` and `/console taintLog 2`, then `/reload`. Record build, server environment, date, locales, roles, selected supported raid, and selected already-catalogued fallback encounter without private character/account identifiers.
2. Put one English authority client and one available supported non-English member client in the same real group/raid. Verify both resolve the same canonical supported raid while retaining their localized display names.
3. Use one exact real server-emitted fallback yell from the verified catalog in its expected canonical raid and verify the boss records. Verify altered text does not record and the same exact text outside the expected raid does not record; synthetic debug injection is not evidence.
4. Exercise the normal raid-history share/sync path and confirm the recognizable sanitized record converges once on both clients with no protocol error. In quarantine, confirm raid-history sync remains unavailable while Reserves and Distribution remain independent.
5. From the member, run `/rma reserves sync`, repeat immediately, then repeat after at least five seconds. Confirm the first and post-expiry compatible requests produce response work and visible convergence, while the immediate equivalent repeat produces no duplicate response work. Compare `/rma reserves meta` and visible reserve state.
6. From the member, invoke `/run RMA.Services.Loot.DistributionSession.RequestSnapshot()` with the same first/immediate/post-five-second timing. Confirm first and post-expiry authority snapshot responses and visible convergence, with no duplicate response work for the immediate repeat.
7. During combat, exercise `/rma`, minimap/QuickBar, principal window open/close, selection/refresh, and normal synchronization requests. Illegal operations must defer or reject safely; do not force casts, targeting, macros, item use, or other protected automation.
8. After combat/logout, inspect only the fresh session's script errors and taint log. Require zero new RMA-attributable Lua errors, `ADDON_ACTION_BLOCKED`, `ADDON_ACTION_FORBIDDEN`, protected-action failures, or attributable taint lines.
9. Supply `Actual`, `PASS`/`FAIL`, and concise privacy-safe evidence for every remaining checklist row. Missing locale, second client, encounter, or log environment remains PENDING.
  </how-to-verify>
  <resume-signal>Reply with all numbered observed results and sanitized evidence. A bare approval is insufficient. The executor records only user-observed results, verifies every checklist row is PASS, and runs `git diff --check`; any PENDING/FAIL keeps QUAL-03 and Phase 4 open. For an evidenced failure, apply no speculative fix: stop for a minimal gap plan and allow at most two focused live repair cycles for the same cause without new evidence.</resume-signal>
  <verify>Read the final checklist and confirm every localized-raid, scoped-yell, raid-history, Reserves, Distribution, combat, Lua/protected-error, and fresh-taint row has human-supplied Actual and Evidence plus Status PASS. Search for `PENDING|FAIL`; zero required rows may match. Run `git diff --check` and confirm no WTF file, full log, or private identifier is tracked.</verify>
  <done>Every QUAL-03 row is explicitly human-observed PASS and the privacy-safe checklist is the only committed live evidence; otherwise Phase 4 remains blocked.</done>
</task>

</tasks>

<verification>
- Plan 04-01 automated evidence is referenced but never used to fill a live row.
- Three isolated SavedVariables profiles and recovery are human-observed with the client closed for every copy/edit step and only sanitized evidence committed.
- The localized raid/yell and all three synchronization paths are observed between two real clients; Reserves and Distribution include first/immediate/post-five-second timing.
- Combat and taint acceptance comes from a clean exact-session log and normal RMA operations, not synthetic helpers or waived warnings.
- Every required checklist row is PASS; otherwise the checkpoint remains blocking and no phase completion is claimed.
</verification>

<success_criteria>
- QUAL-03 has a complete, repeatable, privacy-safe human-observed checklist with no PENDING or FAIL row.
- Unsupported archives remain preserved through quarantine and valid-backup restoration automatically removes degraded state.
- Real localized, multi-client, combat, error, and taint behavior matches the milestone contracts without wire or persistence changes.
</success_criteria>

<output>
After every blocking checkpoint passes and the checklist contains only human-observed PASS results, create `.planning/phases/04-milestone-verification/04-02-SUMMARY.md`. If any row remains PENDING or FAIL, do not create a completion summary or claim QUAL-03 complete.
</output>
